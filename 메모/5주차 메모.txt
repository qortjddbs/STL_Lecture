void (*a)() = f;        // a는 함수 f를 가리키는 포인터

a(); == f(); 

*a 를 무조건 소괄호로 감싸줘야 함! 안감싸면 void* 를 반환하는 함수라고 생각함!!

점프는 함수!
(*점프)();        // 이게 진짜 함수호출
// 점프()         // 이건 야매로 하는 호출!
"점프"라는 함수 주소에 가서 *로 값을 얻어오고 그걸 실행() 하라!!

    []() {
        std::cout << "안녕 난 람다야 반가워" << std::endl;
        };
-> 의 결과는?? 출력 안됨.
    []() {
        std::cout << "안녕 난 람다야 반가워" << std::endl;
        }();
해줘야 진짜 출력됨. 위의 코드는 만들어놓고 호출을 안한 것!

sort(a.begin(), a.end());       // 3번째 인자에 int 기준 오름차순 이 기본으로 깔림.
int 기준 오름차순은 return a<b; 를 리턴하고 (bool 자료형) callable타입. (함수 포인터, 람다, 함수 객체 등)

sort가 이름공간 std에 있는데 앞에 std::를 안붙여줘도 잘 실행되는 이유 : ADL 덕분
























