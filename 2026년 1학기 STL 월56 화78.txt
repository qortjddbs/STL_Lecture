//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 3일																					(1주 1일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.24 (17.14.27로 맞추기!)
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

int main()
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자

void save(const std::string& fname)
{
	std::ifstream in{ fname };
	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };

	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostream_iterator<char>{out});
}//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 3일																					(1주 1일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	
	
	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };

	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostream_iterator<char>{out});
}//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 3일																					(1주 1일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 9일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	
	
	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };

	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostream_iterator<char>{out});
}//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 3일																					(1주 1일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 9일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	
	
	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };

	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostream_iterator<char>{out});
}//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 3일																					(1주 1일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 9일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	
	
	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };
	// if (not out)		이건 할 필요 없음. std::ios::app -> 옛날 파일 그대로 두고 덮어쓰는 모드

	//std::copy(std::istreambuf_iterator<char>{in}, {},
	//	std::ostream_iterator<char>{out});

	char c;
	in >> std::noskipws;		// 이렇게 쓰면 공백 포함
	while(in >> c)		// operator>>(in, c); 이렇게 바뀜. (연산자 오버로딩)
		out << c;
}//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 3일																					(1주 1일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 9일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	
	
	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };
	// if (not out)		이건 할 필요 없음. std::ios::app -> 옛날 파일 그대로 두고 덮어쓰는 모드

	//std::copy(std::istreambuf_iterator<char>{in}, {},
	//	std::ostream_iterator<char>{out});

	char c;
	in >> std::noskipws;		// 이렇게 쓰면 공백 포함
	while(in >> c)		// operator>>(in, c); 이렇게 바뀜. (연산자 오버로딩)
		out << c;		// 파일에 직접 쓰기
}

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 9일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	
	
	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };
	// if (not out)		이건 할 필요 없음. std::ios::app -> 옛날 파일 그대로 두고 덮어쓰는 모드

	// std::copy( 어디부터, 어디까지를, 어디로 );

	std::copy(std::istreambuf_iterator<char>{in}, {},		// iterator in파일로부터 char, {} -> EOF, 마지막 글자까지 (특별한 약속)
		std::ostream_iterator<char>{out});

	//char c;
	//in >> std::noskipws;		// 이렇게 쓰면 공백 포함
	//while(in >> c)		// operator>>(in, c); 이렇게 바뀜. (연산자 오버로딩)
	//	out << c;		// 파일에 직접 쓰기
}//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 9일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	std::cout << "\"" << fname << "\"를 저장합니다" << '\n';


	
	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };
	// if (not out)		이건 할 필요 없음. std::ios::app -> 옛날 파일 그대로 두고 덮어쓰는 모드

	// std::copy( 어디부터, 어디까지를, 어디로 );
	// buf없으면 글자'만' 읽어옴. buf 붙이면 화면에 있는 내용 그대로 옮기라는 뜻.
	std::copy(std::istreambuf_iterator<char>{in}, {},		// iterator in파일로부터 char, {} -> EOF, 마지막 글자까지 (특별한 약속)
		std::ostream_iterator<char>{out});

	//char c;
	//in >> std::noskipws;		// 이렇게 쓰면 공백 포함
	//while(in >> c)		// operator>>(in, c); 이렇게 바뀜. (연산자 오버로딩)
	//	out << c;		// 파일에 직접 쓰기
}

==========================================================================================
저장한 시간 - 
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 9일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	std::cout << "\"" << fname << "\"를 저장합니다" << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };
	// if (not out)		이건 할 필요 없음. std::ios::app -> 옛날 파일 그대로 두고 덮어쓰는 모드

	out << '\n' << '\n';
	out << "==========================================================================================" << '\n';
	out << "저장한 시간 - " << '\n';
	out << "==========================================================================================" << '\n';
	out << '\n';

	// std::copy( 어디부터, 어디까지를, 어디로 );
	// buf없으면 글자'만' 읽어옴. buf 붙이면 화면에 있는 내용 그대로 옮기라는 뜻.
	std::copy(std::istreambuf_iterator<char>{in}, {},		// iterator in파일로부터 char, {} -> EOF, 마지막 글자까지 (특별한 약속)
		std::ostream_iterator<char>{out});

	//char c;
	//in >> std::noskipws;		// 이렇게 쓰면 공백 포함
	//while(in >> c)		// operator>>(in, c); 이렇게 바뀜. (연산자 오버로딩)
	//	out << c;		// 파일에 직접 쓰기
}

==========================================================================================
저장한 시간 - 
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 9일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	std::cout << "\"" << fname << "\"를 저장합니다" << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };
	// if (not out)		이건 할 필요 없음. std::ios::app -> 옛날 파일 그대로 두고 덮어쓰는 모드

	// 시간정보를 얻는다
	auto now = std::chrono::system_clock::now();		// time epoch로 부터 1초마다 증가한 tick 수
	// 데이터 타입은 컴파일러가 추측할 수 있기 때문에(마우스 갖다대면 알려줌) 직접 써줘도 되고 auto를 써서 컴파일러가 자동으로 써주게끔 할 수 있음.

	std::cout << now << '\n';

	out << '\n' << '\n';
	out << "==========================================================================================" << '\n';
	out << "저장한 시간 - " << '\n';
	out << "==========================================================================================" << '\n';
	out << '\n';

	// std::copy( 어디부터, 어디까지를, 어디로 );
	// buf없으면 글자'만' 읽어옴. buf 붙이면 화면에 있는 내용 그대로 옮기라는 뜻.
	std::copy(std::istreambuf_iterator<char>{in}, {},		// iterator in파일로부터 char, {} -> EOF, 마지막 글자까지 (특별한 약속)
		std::ostream_iterator<char>{out});

	//char c;
	//in >> std::noskipws;		// 이렇게 쓰면 공백 포함
	//while(in >> c)		// operator>>(in, c); 이렇게 바뀜. (연산자 오버로딩)
	//	out << c;		// 파일에 직접 쓰기
}

==========================================================================================
저장한 시간 - 
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 9일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	std::cout << "\"" << fname << "\"를 저장합니다" << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };
	// if (not out)		이건 할 필요 없음. std::ios::app -> 옛날 파일 그대로 두고 덮어쓰는 모드

	// 시간정보를 얻는다
	auto now = std::chrono::system_clock::now();		// time epoch로 부터 1초마다 증가한 tick 수
	// 데이터 타입은 컴파일러가 추측할 수 있기 때문에(마우스 갖다대면 알려줌) 직접 써줘도 되고 auto를 써서 컴파일러가 자동으로 써주게끔 할 수 있음.

	std::cout << now << '\n';

	out << '\n' << '\n';
	out << "==========================================================================================" << '\n';
	out << "저장한 시간 - " << '\n';
	out << "==========================================================================================" << '\n';
	out << '\n';

	// std::copy( 어디부터, 어디까지를, 어디로 );
	// buf없으면 글자'만' 읽어옴. buf 붙이면 화면에 있는 내용 그대로 옮기라는 뜻.
	std::copy(std::istreambuf_iterator<char>{in}, {},		// iterator in파일로부터 char, {} -> EOF, 마지막 글자까지 (특별한 약속)
		std::ostream_iterator<char>{out});

	//char c;
	//in >> std::noskipws;		// 이렇게 쓰면 공백 포함
	//while(in >> c)		// operator>>(in, c); 이렇게 바뀜. (연산자 오버로딩)
	//	out << c;		// 파일에 직접 쓰기
}

시간형식 / 통화형식

==========================================================================================
저장한 시간 - 2026-03-09 14:22:00.4361075
저장한 시간 - 2026년 3월 9일 오후 2:22 로 바꿔보기.
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 9일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	std::cout << "\"" << fname << "\"를 저장합니다" << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };
	// if (not out)		이건 할 필요 없음. std::ios::app -> 옛날 파일 그대로 두고 덮어쓰는 모드

	// 시간정보를 얻는다
	auto now = std::chrono::system_clock::now();		// time epoch로 부터 1초마다 증가한 tick 수
	// 데이터 타입은 컴파일러가 추측할 수 있기 때문에(마우스 갖다대면 알려줌) 직접 써줘도 되고 auto를 써서 컴파일러가 자동으로 써주게끔 할 수 있음.

	using namespace std::chrono_literals;
	// out << now + 9h << '\n';		// 여기서 now는 그리니치 천문대 기준 시간 (우리나라로 바꾸려면 +9시간 해줘야함)

	out << '\n' << '\n';
	out << "==========================================================================================" << '\n';
	out << "저장한 시간 - " << now + 9h << '\n';
	out << "==========================================================================================" << '\n';
	out << '\n';

	// std::copy( 어디부터, 어디까지를, 어디로 );
	// buf없으면 글자'만' 읽어옴. buf 붙이면 화면에 있는 내용 그대로 옮기라는 뜻.
	std::copy(std::istreambuf_iterator<char>{in}, {},		// iterator in파일로부터 char, {} -> EOF, 마지막 글자까지 (특별한 약속)
		std::ostream_iterator<char>{out});

	//char c;
	//in >> std::noskipws;		// 이렇게 쓰면 공백 포함
	//while(in >> c)		// operator>>(in, c); 이렇게 바뀜. (연산자 오버로딩)
	//	out << c;		// 파일에 직접 쓰기
}

==========================================================================================
저장한 시간 - 2026-03-09 14:45:37.2371114
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// save 파일 분리
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "save를 파일로 분리하였다" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌.
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

==========================================================================================
저장한 시간 - 2026-03-09 14:52:14.1940674
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
// save 파일 분리
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "save를 파일로 분리하였다" << '\n';
	
	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-09 14:52:50.1417031
==========================================================================================

// -------------------------------------------------------------------------------------------------
// 한 학기 강의를 저장하기 위한 함수
// 
// 2026. 03. 09 (월)																																					백성윤
// -------------------------------------------------------------------------------------------------
#pragma once

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

==========================================================================================
저장한 시간 - 2026-03-09 14:52:50.1517691
==========================================================================================

// -------------------------------------------------------------------------------------------------
// 한 학기 강의를 저장하기 위한 함수
// 
// 2026. 03. 09 (월)																																					백성윤
// -------------------------------------------------------------------------------------------------
// fname부터 하자 -> naming convention
#include <iostream>
#include <string>
#include <fstream>
#include <chrono>
// 헤더파일은 맨 나중에 넣기. 오류날 수 있음 (찾기 힘듦)
#include "save.h"

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}

	std::cout << "\"" << fname << "\"를 저장합니다" << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };
	// if (not out)		이건 할 필요 없음. std::ios::app -> 옛날 파일 그대로 두고 덮어쓰는 모드

	// 시간정보를 얻는다
	auto now = std::chrono::system_clock::now();		// time epoch로 부터 1초마다 증가한 tick 수
	// 데이터 타입은 컴파일러가 추측할 수 있기 때문에(마우스 갖다대면 알려줌) 직접 써줘도 되고 auto를 써서 컴파일러가 자동으로 써주게끔 할 수 있음.

	using namespace std::chrono_literals;
	// out << now + 9h << '\n';		// 여기서 now는 그리니치 천문대 기준 시간 (우리나라로 바꾸려면 +9시간 해줘야함)

	out << '\n' << '\n';
	out << "==========================================================================================" << '\n';
	out << "저장한 시간 - " << now + 9h << '\n';
	out << "==========================================================================================" << '\n';
	out << '\n';

	// std::copy( 어디부터, 어디까지를, 어디로 );
	// buf없으면 글자'만' 읽어옴. buf 붙이면 화면에 있는 내용 그대로 옮기라는 뜻.
	std::copy(std::istreambuf_iterator<char>{in}, {},		// iterator in파일로부터 char, {} -> EOF, 마지막 글자까지 (특별한 약속)
		std::ostream_iterator<char>{out});

	//char c;
	//in >> std::noskipws;		// 이렇게 쓰면 공백 포함
	//while(in >> c)		// operator>>(in, c); 이렇게 바뀜. (연산자 오버로딩)
	//	out << c;		// 파일에 직접 쓰기
}

==========================================================================================
저장한 시간 - 2026-03-09 14:52:50.1615429
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
// save 파일 분리
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "save를 파일로 분리하였다" << '\n';
	
	save("save.h");
	save("save.cpp");
	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-09 14:55:15.8959653
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"
// using namespace std;		// 강의 시 가독성을 위해 사용, 나는 이렇게 코딩하지 말자

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-09 14:56:39.6176200
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	// [문제] 의도대로 실행되게 하자
	int a{ 1 }, b{ 2 };

	// 여기에서 change를 호출하였다.

	std::cout << a << ", " << b << std::endl;		// 2, 1이라고 출력되어야 함

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-09 14:58:09.1715802
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

void change(int &a, int &b);

// ----------
int main()
// ----------
{
	// [문제] 의도대로 실행되게 하자
	int a{ 1 }, b{ 2 };

	// 여기에서 change를 호출하였다.
	change(a, b);

	std::cout << a << ", " << b << std::endl;		// 2, 1이라고 출력되어야 함

	save("메인.cpp");
}

void change(int &a, int &b) {
	int temp = a;
	a = b;
	b = temp;
}

==========================================================================================
저장한 시간 - 2026-03-09 15:06:57.9356357
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

void change(int &a, int &b);

// ----------
int main()
// ----------
{
	// 얘네는 코드 세그먼트
	std::cout << "main - " << std::addressof(main) << std::endl;
	std::cout << "save - " << std::addressof(save) << std::endl;

	// [문제] 의도대로 실행되게 하자
	int a{ 1 }, b{ 2 };

	// 얘네는 스택
	std::cout << "a - " << std::addressof(a) << std::endl;
	std::cout << "b - " << std::addressof(b) << std::endl;

	// 여기에서 change를 호출하였다.
	// change란? 호출할 수 있는 어떤 것. (호출할 만한게 무한대임) -> 호출 연산자는 ()
	change(a, b);

	std::cout << a << ", " << b << std::endl;		// 2, 1이라고 출력되어야 함

	save("메인.cpp");
}

void change(int &a, int &b) {
	int temp = a;
	a = b;
	b = temp;
}

==========================================================================================
저장한 시간 - 2026-03-09 15:08:19.4433843
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

void change(int &a, int &b);

int a{ 1 };

// ----------
int main()
// ----------
{
	// 얘네는 코드 세그먼트
	std::cout << "main - " << std::addressof(main) << std::endl;
	std::cout << "save - " << std::addressof(save) << std::endl;

	// [문제] 의도대로 실행되게 하자
	int b{ 2 };

	// 얘네는 스택
	std::cout << "a - " << std::addressof(a) << std::endl;		// 전역으로 옮기면 코드 세그먼트로 바뀜
	std::cout << "b - " << std::addressof(b) << std::endl;

	// 여기에서 change를 호출하였다.
	// change란? 호출할 수 있는 어떤 것. (호출할 만한게 무한대임) -> 호출 연산자는 ()
	change(a, b);

	std::cout << a << ", " << b << std::endl;		// 2, 1이라고 출력되어야 함

	save("메인.cpp");
}

void change(int &a, int &b) {
	int temp = a;
	a = b;
	b = temp;
}

==========================================================================================
저장한 시간 - 2026-03-09 15:20:01.4896362
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

void change(int *, int *);

// ----------
int main()
// ----------
{
	// 얘네는 코드 세그먼트
	//std::cout << "main - " << std::addressof(main) << std::endl;
	//std::cout << "save - " << std::addressof(save) << std::endl;
	{
		// [문제] 의도대로 실행되게 하자
		// Dog는 class로 작성하자.
		using Dog = int;		// #define Dog int 이건 사용하면 안된다
		Dog a{ 1 }, b{ 2 };

		// 얘네는 스택
		//std::cout << "a - " << std::addressof(a) << std::endl;		// 전역으로 옮기면 main, save의 윗동네로 변경
		//std::cout << "b - " << std::addressof(b) << std::endl;

		// 여기에서 change를 호출하였다.
		// change란? 호출할 수 있는 어떤 것. (호출할 만한게 무한대임) -> 호출 연산자는 ()
		// change(a, b); -> 이것도 맞지만 돌아가는 답임. 컴파일러가 알아서 주소로 변환해서 돌아가게 해줌. (항상 교수님이 알려준 코드처럼 코딩하자 포인터로)
		// 레퍼런스는 실제로 존재하지 않는다!!!!!! (찾아보기)
		change(&a, &b);

		std::cout << a << ", " << b << std::endl;		// 2, 1이라고 출력되어야 함
	}

	{
		// [문제] 의도대로 실행되게 하자
		int a{ 1 }, b{ 2 };

		// 얘네는 스택
		//std::cout << "a - " << std::addressof(a) << std::endl;		// 전역으로 옮기면 main, save의 윗동네로 변경
		//std::cout << "b - " << std::addressof(b) << std::endl;

		// 여기에서 change를 호출하였다.
		// change란? 호출할 수 있는 어떤 것. (호출할 만한게 무한대임) -> 호출 연산자는 ()
		// change(a, b); -> 이것도 맞지만 돌아가는 답임. 컴파일러가 알아서 주소로 변환해서 돌아가게 해줌. (항상 교수님이 알려준 코드처럼 코딩하자 포인터로)
		// 레퍼런스는 실제로 존재하지 않는다!!!!!! (찾아보기)
		change(&a, &b);

		std::cout << a << ", " << b << std::endl;		// 2, 1이라고 출력되어야 함
	}

	save("메인.cpp");
}

void change(int *p, int *q) {
	int temp{ *p };
	*p = *q;
	*q = temp;
}

==========================================================================================
저장한 시간 - 2026-03-10 15:52:26.8049560
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

class Dog;			// forward declaration

void change(int *, int *);

class Dog {		// 중괄호는 여기에 쓰자! (좋은 코드 습관)
private:
	int num;

public:
	Dog() = default;		// 디폴트 생성자 만들어주기!
	explicit Dog(int n) : num{ n } { }
	// ~Dog() 쓸데없이 소멸자 만들지말기 (0점짜리)

	// 이 코드는 사용하지 말자
	//operator int () {
	//	return num;
	//}


	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		return os << dog.num;
	}
};

void change(Dog& p, Dog& q) {
	Dog temp{ p };		// temp를 "복사 생성"하라	- 내가 안만들어도 돌아가는 이유 => 스페셜 함수이기 때문! 컴파일러가 알아서 만들어줌
	p = q;		// p.operator = (q); -> 스페셜 함수!! 자동으로 만들어서 돌아가게 해줌
	q = temp;
}

// ----------
int main()
// ----------
{
	// 얘네는 코드 세그먼트
	//std::cout << "main - " << std::addressof(main) << std::endl;
	//std::cout << "save - " << std::addressof(save) << std::endl;
	{
		// [문제] 의도대로 실행되게 하자
		// Dog는 class로 작성하자.
		// using Dog = int;		// #define Dog int 이건 사용하면 안된다
		Dog a{ 1 }, b{ 2 };		// Dog a = 1, b = 2; 이런식으로 쓰는 건 좀 "위험" 하다
		// Dog c = 1;		// 이렇게 사용하면 내가 원치않게 실행될 수 있음. 컴파일러가 알아서 형변환 실행 (그래서 explicit 붙이면 바로 오류)

		// 얘네는 스택
		//std::cout << "a - " << std::addressof(a) << std::endl;		// 전역으로 옮기면 main, save의 윗동네로 변경
		//std::cout << "b - " << std::addressof(b) << std::endl;

		// 여기에서 change를 호출하였다.
		// change란? 호출할 수 있는 어떤 것. (호출할 만한게 무한대임) -> 호출 연산자는 ()
		// change(a, b); -> 이것도 맞지만 돌아가는 답임. 컴파일러가 알아서 주소로 변환해서 돌아가게 해줌. (항상 교수님이 알려준 코드처럼 코딩하자 포인터로)
		// 레퍼런스는 실제로 존재하지 않는다!!!!!! (찾아보기)
		change(a, b);

		std::cout << a << ", " << b << std::endl;		// 2, 1이라고 출력되어야 함
	}

	{
		// [문제] 의도대로 실행되게 하자
		int a{ 1 }, b{ 2 };

		// 얘네는 스택
		//std::cout << "a - " << std::addressof(a) << std::endl;		// 전역으로 옮기면 main, save의 윗동네로 변경
		//std::cout << "b - " << std::addressof(b) << std::endl;

		// 여기에서 change를 호출하였다.
		// change란? 호출할 수 있는 어떤 것. (호출할 만한게 무한대임) -> 호출 연산자는 ()
		// change(a, b); -> 이것도 맞지만 돌아가는 답임. 컴파일러가 알아서 주소로 변환해서 돌아가게 해줌. (항상 교수님이 알려준 코드처럼 코딩하자 포인터로)
		// 레퍼런스는 실제로 존재하지 않는다!!!!!! (찾아보기)
		change(&a, &b);

		std::cout << a << ", " << b << std::endl;		// 2, 1이라고 출력되어야 함
	}

	save("메인.cpp");
}

void change(int *p, int *q) {
	int temp{ *p };
	*p = *q;
	*q = temp;
}

==========================================================================================
저장한 시간 - 2026-03-10 16:02:40.8034691
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

void change(std::string);
void change(int, int) {
	std::cout << "int 2개를 인자로" << std::endl;
}

// ----------
int main()
// ----------
{
	change(1, 2);

	save("메인.cpp");
}


==========================================================================================
저장한 시간 - 2026-03-10 16:04:30.2802691
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

void change(std::string);

void change(int, int) {
	std::cout << "int 2개를 인자로" << std::endl;
}

// ----------
int main()
// ----------
{
	change(1, 2);
	//change("12345 ");		// 돌려보면 컴파일은 되는데 링크 단계에서 오류 발생 (change@@YAXC ~~ 라는 이름의 함수를 지가 알아서 만듦!)

	save("메인.cpp");
}


==========================================================================================
저장한 시간 - 2026-03-10 16:46:39.5635830
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없다!
template<class T>
void change(T& p, T& q) {									// 자동화
	T temp{ p };				
	p = q;
	q = temp;
}

// double에 대한 sepcialization
template<>
void change<double>(double& p, double& q) {									// 자동화
	std::cout << "컴파일러가 찍어낸 코드" << std::endl;
	double temp{ p };
	p = q;
	q = temp;
}

// ----------
int main()
// ----------
{
	double a, b;
	change<double>(a, b);							// 뭘 호출해야 하나? - 호출우선순위 결정

	// change("S", "T");

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-10 16:52:58.5949239
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없다!
template<class T>
void change(T& p, T& q) {									// 자동화
	T temp{ p };				
	p = q;
	q = temp;
}

// double에 대한 sepcialization
template<>
void change<double>(double& p, double& q) {									// 자동화
	std::cout << "컴파일러가 찍어낸 코드" << std::endl;
	double temp{ p };
	p = q;
	q = temp;
}

// ----------
int main()
// ----------
{
	double a, b;
	change<double>(a, b);							// 뭘 호출해야 하나? - 호출우선순위 결정

	std::string s{ "2026년" }, t{ "3월10일" };
	change(s, t);

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-10 16:53:48.6271206
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없다!
template<class T>
void change(T& p, T& q) {									// 자동화
	std::cout << "이 문장을 보면 컴파일러가 열일한줄 알아라" << '\n';
	T temp{ p };				
	p = q;
	q = temp;
}

// double에 대한 sepcialization
template<>
void change<double>(double& p, double& q) {									// 자동화
	std::cout << "컴파일러가 찍어낸 코드" << std::endl;
	double temp{ p };
	p = q;
	q = temp;
}

// ----------
int main()
// ----------
{
	double a, b;
	change<double>(a, b);							// 뭘 호출해야 하나? - 호출우선순위 결정

	std::string s{ "2026년" }, t{ "3월10일" };
	change(s, t);

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-10 16:54:29.0132084
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없다!
template<class T>
void change(T& p, T& q) {									// 자동화
	std::cout << "이 문장을 보면 컴파일러가 열일한줄 알아라" << '\n';
	T temp{ p };				
	p = q;
	q = temp;
}

// double에 대한 sepcialization
//template<>
//void change<double>(double& p, double& q) {									// 자동화
//	std::cout << "컴파일러가 찍어낸 코드" << std::endl;
//	double temp{ p };
//	p = q;
//	q = temp;
//}

// ----------
int main()
// ----------
{
	double a, b;
	change<double>(a, b);							// 뭘 호출해야 하나? - 호출우선순위 결정

	std::string s{ "2026년" }, t{ "3월10일" };
	change(s, t);

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-10 17:06:52.7982696
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

template<class T>
T add(T a, T b) {
	return a + b;
}

// ----------
int main()
// ----------
{
	// 템플릿 함수 add를 한 개 만들어 실행되게 하라.
	std::cout << add<int>(1, 2) << std::endl;

	// std::cout << add( "2026년"s , "3월10일"s ) << std::endl;

	std::cout << add<std::string>(std::string{ "2026년" }, std::string{ "3월10일" }) << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-10 17:07:28.6019714
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

template<class T>
T add(T a, T b) {
	return a + b;
}

// ----------
int main()
// ----------
{
	// 템플릿 함수 add를 한 개 만들어 실행되게 하라.
	std::cout << add<int>(1, 2) << std::endl;

	// std::cout << add( "2026년"s , "3월10일"s ) << std::endl;

	std::cout << add<std::string>(std::string{ "2026년 " }, std::string{ "3월 10일" }) << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:38:42.4965294
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std:: uniform_int_distribution uid;

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	for (int i = 0; i < 100; ++i) {
		std::cout << uid(dre) << std::endl;

	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:39:54.7362718
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, std::numeric_limits<int>::max()};

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	for (int i = 0; i < 100; ++i) {
		std::cout << uid(dre) << std::endl;

	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:40:41.6336575
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ std::numeric_limits<int>::min(), std::numeric_limits<int>::max()};

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	for (int i = 0; i < 100; ++i) {
		std::cout << uid(dre) << std::endl;

	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:41:14.5359003
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	for (int i = 0; i < 1000; ++i) {
		std::cout << uid(dre) << std::endl;

	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:41:32.2813724
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	for (int i = 0; i < 1000; ++i) {
		std::cout << uid(dre) << " ";

	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:42:52.5843581
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	for (int i = 0; i < 1000; ++i) {
		printf("{:8}", uid(dre));
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:43:15.5185315
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	for (int i = 0; i < 1000; ++i) {
		std::print("{:8}", uid(dre));
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:43:36.1036368
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	for (int i = 0; i < 1000; ++i) {
		std::print("{:8}", uid(dre));
	}
	std::cout << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:43:51.2068746
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	for (int i = 0; i < 1000; ++i) {
		std::print("{:<8}", uid(dre));
	}
	std::cout << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:48:38.8858554
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:50:49.4005227
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하시오.
	int max = -1;
	for (int i = 0; i < 1000; ++i) {
		if (max < a[i]) max = a[i];
	}

	std::cout << "가장 큰 값: " << max << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:50:55.4798215
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하시오.
	int max = -1;
	for (int i = 0; i < 1000; ++i) {
		if (max < a[i]) max = a[i];
	}

	std::cout << "가장 큰 값: " << max << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:50:57.6154344
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하시오.
	int max = -1;
	for (int i = 0; i < 1000; ++i) {
		if (max < a[i]) max = a[i];
	}

	std::cout << "가장 큰 값: " << max << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:51:00.4207794
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하시오.
	int max = -1;
	for (int i = 0; i < 1000; ++i) {
		if (max < a[i]) max = a[i];
	}

	std::cout << "가장 큰 값: " << max << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:53:43.7182732
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하시오.
	int max = -1;
	for (int num : a) {
		if (max < num) max = num;
	}

	std::cout << "가장 큰 값: " << max << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:55:10.4213197
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하시오.
	int max = -1;
	for (const int& num : a) {
		if (max < num) max = num;
	}

	std::cout << "가장 큰 값: " << max << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:55:37.4034269
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000]{ uid(dre) };

	//for (int& num : a) {
	//	num = uid(dre);
	//}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하시오.
	int max = -1;
	for (const int& num : a) {
		if (max < num) max = num;
	}

	std::cout << "가장 큰 값: " << max << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 14:02:06.4748435
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하시오.
	int maxVal{ std::numeric_limits<int>::min() };

	for (int num : a) {
		if (maxVal < num) maxVal = num;
	}

	std::cout << "가장 큰 값: " << maxVal<< std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 14:03:29.9138231
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하시오.
	std::sort(std::begin(a), std::end(a));
	std::cout << "가장 큰 값 - " << a[1000 - 1] << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 14:04:27.8766515
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하라.
	// 이런 답은 0점. 쓸데없는 일을 너무 많이 함. (컴퓨터 학대)
	std::sort(std::begin(a), std::end(a));
	std::cout << "가장 큰 값 - " << a[1000 - 1] << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 14:15:48.1266414
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하라.
	std::max_element(std::begin(a), std::end(a));

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 14:19:34.7408900
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하라.
	int* p = std::max_element( &a[0], &a[1000]);
	std::cout << "가장 큰 값은: " << *p << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 14:21:07.0598259
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하라.
	int* p = std::max_element( &a[0], &a[1000]);		// 납땜 코드 (hard-wired code)
	std::cout << "가장 큰 값은: " << *p << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 14:24:38.9226761
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하라.
	int* p = std::max_element( std::begin(a), std::end(a));
	std::cout << "가장 큰 값은: " << *p << std::endl;
	// 알고리즘을 이용하는 게 실수할 부분이 훨씬 적고, 일단 max 알고리즘은 병렬처리가 가능한 게 큼.

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 14:40:59.2598444
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//------------------------------------------------------------------------------------------------------- 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include "save.h"

// random_device - 마우스의 위치, cpu의 온도, 팬이 돌아가는 속도 등등을 고려 (완전한 랜덤)
std::default_random_engine dre{ std::random_device{}()};
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하라.
	int* p = std::max_element( std::begin(a), std::end(a));
	 std::cout << "가장 큰 값은: " << *p << std::endl;
	// 알고리즘을 이용하는 게 실수할 부분이 훨씬 적고, 일단 max 알고리즘은 병렬처리가 가능한 게 큼.

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 14:46:30.0026174
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//------------------------------------------------------------------------------------------------------- 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 작은 값과 큰 값을 찾아서 화면에 출력하라.
	int* min_value = std::min_element(std::begin(a), std::end(a));
	int* max_value = std::max_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값: " << *min_value << std::endl;
	std::cout << "가장 큰 값: " << *max_value << std::endl;


	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 14:57:40.6946809
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//------------------------------------------------------------------------------------------------------- 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 작은 값과 큰 값을 찾아서 화면에 출력하라.
	std::pair<int*, int*>result = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값: " << *(result.first) << ", 가장 큰 값: " << *(result.second) << std::endl;
	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 15:01:23.1667366
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//------------------------------------------------------------------------------------------------------- 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 작은 값과 큰 값을 찾아서 화면에 출력하라.
	// syntatic sugar 인 structured binding을 사용하면 pair를 이렇게 쓸 수 있다!
	auto[작은값, 큰값] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값: " << 작은값 << ", 가장 큰 값: " << 큰값 << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 15:01:38.0581129
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//------------------------------------------------------------------------------------------------------- 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 작은 값과 큰 값을 찾아서 화면에 출력하라.
	// syntatic sugar 인 structured binding을 사용하면 pair를 이렇게 쓸 수 있다!
	auto[작은값, 큰값] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값: " << *(작은값) << ", 가장 큰 값: " << *(큰값) << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 15:01:51.9729281
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//------------------------------------------------------------------------------------------------------- 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 작은 값과 큰 값을 찾아서 화면에 출력하라.
	// syntatic sugar 인 structured binding을 사용하면 pair를 이렇게 쓸 수 있다!
	auto[작은값, 큰값] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값: " << *작은값 << ", 가장 큰 값: " << *큰값 << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 15:08:22.0218464
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//------------------------------------------------------------------------------------------------------- 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include <array>		// C++ 11
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	// int[1000] 이런 자료구조는 앞으론 쓰지 말 것		new int[1000] 이거 쓸 때 말고는 안쓸거임 (이건 기계어 수준이라 안쓸 수 없음)
	// 대체할 수 있는 더 좋은 것이 있기 때문 - 시간/공간의 zero-overhead
	// 컴퓨터 세상에서 더 좋은 것 - 메모리를 덜 잡아먹음. 시간이 덜 걸림.
	// array<T, N>

	// int a[1000];		// 자료형은 int[N] 인데, 이름위치가 비정상

	std::array<int, 1000> a;

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 작은 값과 큰 값을 찾아서 화면에 출력하라.
	// syntatic sugar 인 structured binding을 사용하면 pair를 이렇게 쓸 수 있다!
	auto[작은값, 큰값] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값: " << *작은값 << ", 가장 큰 값: " << *큰값 << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 15:10:34.2820448
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//------------------------------------------------------------------------------------------------------- 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include <array>		// C++ 11
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	// int[1000] 이런 자료구조는 앞으론 쓰지 말 것		new int[1000] 이거 쓸 때 말고는 안쓸거임 (이건 기계어 수준이라 안쓸 수 없음)
	// 대체할 수 있는 더 좋은 것이 있기 때문 - 시간/공간의 zero-overhead
	// 컴퓨터 세상에서 더 좋은 것 - 메모리를 덜 잡아먹음. 시간이 덜 걸림.
	// array<T, N>

	// int a[1000];		// 자료형은 int[N] 인데, 이름위치가 비정상

	std::array<int, 1000> a;

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	auto[minPos, maxPos] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값: " << *minPos << ", 가장 큰 값: " << *maxPos << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 15:11:49.2972866
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//------------------------------------------------------------------------------------------------------- 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include <array>		// C++ 11
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	std::array<int, 1000> a;
	std::cout << "a가 사용하는 메모리 - " << sizeof a << std::endl;

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	auto[minPos, maxPos] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값: " << *minPos << ", 가장 큰 값: " << *maxPos << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 15:19:51.6916000
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//------------------------------------------------------------------------------------------------------- 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include <array>		// C++ 11
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };
// 다음 시간 멤버 설명부터 다시

template <class T, int N>
class myArray {
public:
	size_t size() const {
		return N;
	}

	int& operator[](int idx) {
		return a[idx];
	}

	T* begin() {
		return &a[0];
	}

	T* end() {
		return &a[N];
	}

private:
	T a[N];
};

// ----------
int main()
// ----------
{
	myArray<int, 1000> a;

	for (int i = 0; i < a.size(); ++i) {
		a[i] = uid(dre);
	}

	//for (int& num : a) {
	//	num = uid(dre);
	//}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	auto[minPos, maxPos] = std::minmax_element(a.begin(), a.end());
	std::cout << "가장 작은 값: " << *minPos << ", 가장 큰 값: " << *maxPos << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-17 16:01:16.4197878
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 17일																					(3주 1일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <random>
#include "save.h"

std:: default_random_engine dre;
std::uniform_int_distribution<int> uid{ std::numeric_limits<int>::min(), std::numeric_limits<int>::max() };

// ----------
int main()
// ----------
{
	// [문제] 랜덤 int값 천만개를 다음과 같이 "int천만개.txt" 파일에 저장하였다.

	std::ofstream out{ "int천만개.txt" };
	for (int i = 0; i < 100; ++i) {
		out << uid(dre) << ' ';
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-17 16:02:44.8381459
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 17일																					(3주 1일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <random>
#include "save.h"

std:: default_random_engine dre;
std::uniform_int_distribution<int> uid{ std::numeric_limits<int>::min(), std::numeric_limits<int>::max() };

// ----------
int main()
// ----------
{
	// [문제] 랜덤 int값 천만개를 다음과 같이 "int천만개.txt" 파일에 저장하였다.

	std::ofstream out{ "int천만개.txt" };		// 1105 bytes
	for (int i = 0; i < 100; ++i) {
		out << uid(dre) << " ";
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-17 16:05:21.2910890
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 17일																					(3주 1일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <random>
#include "save.h"

std:: default_random_engine dre;
std::uniform_int_distribution<int> uid{ std::numeric_limits<int>::min(), std::numeric_limits<int>::max() };

// ----------
int main()
// ----------
{
	// [문제] 랜덤 int값 천만개를 다음과 같이 "int천만개.txt" 파일에 저장하였다.

	std::ofstream out{ "int천만개.txt" };
	for (int i = 0; i < 10'000'000; ++i) {
		out << uid(dre) << std::endl;
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-17 16:07:10.1597230
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 17일																					(3주 1일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <random>
#include "save.h"

std:: default_random_engine dre;
std::uniform_int_distribution<int> uid{ std::numeric_limits<int>::min(), std::numeric_limits<int>::max() };

// ----------
int main()
// ----------
{
	// [문제] 랜덤 int값 천만개를 다음과 같이 "int천만개.txt" 파일에 저장하였다.

	std::ofstream out{ "int천만개.txt" };			// 119'828'581 bytes
	for (int i = 0; i < 100; ++i) {
		out << uid(dre) << '\n';
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-17 16:07:40.4562318
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 17일																					(3주 1일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <random>
#include "save.h"

std:: default_random_engine dre;
std::uniform_int_distribution<int> uid{ std::numeric_limits<int>::min(), std::numeric_limits<int>::max() };

// ----------
int main()
// ----------
{
	// [문제] 랜덤 int값 천만개를 다음과 같이 "int천만개.txt" 파일에 저장하였다.

	std::ofstream out{ "int천만개.txt" };			// 119'828'581 bytes
	for (int i = 0; i < 10'000'000; ++i) {
		out << uid(dre) << '\n';
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-17 16:20:17.2983777
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 17일																					(3주 1일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include "save.h"

// ----------
int main()
// ----------
{
	// [문제] "int천만개.txt" 파일에는 int값 천만개가 저장되어있다.
	// 천만개가 있다는 사실은 틀림없다 - 믿고 쓰자
	// 가장 작은 값을 찾아 출력하라.

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-17 16:41:02.5228628
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 17일																					(3주 1일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <array>
#include <algorithm>
#include "save.h"

std::array<int, 10'000'000> a;

// ----------
int main()
// ----------
{
	// [문제] "int천만개.txt" 파일에는 int값 천만개가 저장되어있다.
	// 천만개가 있다는 사실은 틀림없다 - 믿고 쓰자
	// 가장 작은 값을 찾아 출력하라.
	std::ifstream in{ "int천만개.txt" };
	if (not in) {
		std::cout << "파일 확인해 보세요" << std::endl;
		return 2020317;
	}

	for (int& num : a) {
		in >> num;
	}

	std::cout << "최솟값: " << *min_element (a.begin(), a.end()) << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-17 16:52:31.7137566
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 17일																					(3주 1일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <algorithm>
#include "save.h"

// ----------
int main()
// ----------
{
	// [문제] "int천만개.txt" 파일에는 int값 천만개가 저장되어있다.
	// 천만개가 있다는 사실은 틀림없다 - 믿고 쓰자
	// 가장 작은 값을 찾아 출력하라.
	std::ifstream in{ "int천만개.txt" };
	if (not in) {
		std::cout << "파일 확인해 보세요" << std::endl;
		return 2020317;
	}

	std::cout << "최솟값: " << *min_element(std::istream_iterator<int>{in}, {}) << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-23 13:34:03.5570654
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 23일																					(3주 2일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// ----------
int main()
// ----------
{
	// [문제] int로 표현할 수 있는 값은 -2,147,483,648 ~ +2,147,483,647 까지이다.
	// 모든 int 값을 하나도 빠짐없이 한 개씩 파일에 text로 기록하였다.
	// 값과 값은 빈칸 한 개로 구분하였다.
	// 이렇게 하면 int 를 한 개 기록하는데 평균 몇 바이트가 필요한지 계산하라.
	int num{ 0x01020304 };
	std::cout << num << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-23 13:43:00.8333015
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 23일																					(3주 2일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid;
// ----------
int main()
// ----------
{
	// [문제] 랜덤 int값 1000개를 화면에 출력하라.
	for (int i{}; i < 1000; ++i) {
		std::cout << uid(dre) << std::endl;
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-23 13:45:14.1764712
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 23일																					(3주 2일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <fstream>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid;
// ----------
int main()
// ----------
{
	// [문제] 랜덤 int값 1000'0000개를 파일 "int천만개"에 저장하라.
	std::ofstream out{ "int천만개" };

	for (int i{}; i < 1000'0000; ++i) {
		out << uid(dre) << '\n';
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-23 13:51:11.4071499
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 23일																					(3주 2일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <fstream>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid;
// ----------
int main()
// ----------
{
	// [문제] 랜덤 int값 1000'0000개를 파일 "int천만개"에 저장하라.
	std::ofstream out{ "int천만개" };		
	// 114'826'203 bytes - text mode
	// 4000'0000 - 메모리 그대로 저장한다면 40MB로 충분함.

	int num;
	for (int i{}; i < 1000'0000; ++i) {
		// out << uid(dre) << '\n';
		num = uid(dre);
		out.write(reinterpret_cast<char*>(&num) , sizeof(int));
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-23 13:54:10.4265857
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 23일																					(3주 2일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <fstream>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid;
// ----------
int main()
// ----------
{
	// [문제] 랜덤 int값 1000'0000개를 파일 "int천만개"에 저장하라.
	std::ofstream out{ "int천만개" , std::ios::binary};		
	// 114'826'203 bytes - text mode
	// 4000'0000 - 메모리 그대로 저장한다면 40MB로 충분함.

	int num;
	for (int i{}; i < 1000'0000; ++i) {
		// out << uid(dre) << '\n';
		num = uid(dre);
		out.write(reinterpret_cast<char*>(&num) , sizeof(int));
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-23 13:58:32.4722268
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 23일																					(3주 2일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <filesystem>			// C++ 17
#include "save.h"

// [문제] 파일 "int천만개"에는 int값 천만개가 저장되어 있다.
// 파일은 binary 모드로 열었고 int 값은 stream의 write 함수를 사용하여 저장하였다.

// ----------
int main()
// ----------
{
	std::cout << std::filesystem::file_size("int천만개") / sizeof(int) << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-23 14:20:30.6835090
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 23일																					(3주 2일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <filesystem>			// C++ 17
#include <array>
#include <algorithm>
#include "save.h"

// [문제] 파일 "int천만개"에는 int값 천만개가 저장되어 있다.
// 파일은 binary 모드로 열었고 int 값은 stream의 write 함수를 사용하여 저장하였다.
// 저장된 int값을 모두 메모리에 저장하라.
// 저장된 값 중에서 가장 작은 값과 큰 값을 화면에 출력하라.

std::array<int, 1000'0000> a;

// ----------
int main()
// ----------
{
	std::ifstream in{ "int천만개", std::ios::binary };
	if (not in) {
		std::cout << "파일에 문제가 있다" << std::endl;
		return 20260323;
	}

	in.read( (char*) (a.data()), a.size() * sizeof(int) );

	auto [minPos, maxPos] = minmax_element(a.begin(), a.end());
	std::cout << "작은 값 - " << *minPos << ", 큰 값 - " << *maxPos << std::endl;
	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-23 14:47:05.4040237
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 23일																					(3주 2일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O - class 객체
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"
#include <random>
#include <string>
#include <print>

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };
std::uniform_int_distribution uidNameLen{ 10, 30 };
std::uniform_int_distribution<int> uidChar{ 'a', 'z' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}

private:
	std::string name;		// [10, 30] 사이의 소문자로 구성된 이름
	size_t id;						// [0, 9999]


	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:>4}] - {}", dog.id, dog.name);
		return os;
	}
};

// ----------
int main()
// ----------
{
	Dog dog;

	std::cout << dog << std::endl;
	
	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-23 14:48:00.3082426
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 23일																					(3주 2일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O - class 객체
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"
#include <random>
#include <string>
#include <print>
#include <array>

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };
std::uniform_int_distribution uidNameLen{ 10, 30 };
std::uniform_int_distribution<int> uidChar{ 'a', 'z' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}

private:
	std::string name;		// [10, 30] 사이의 소문자로 구성된 이름
	size_t id;						// [0, 9999]


	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:>4}] - {}", dog.id, dog.name);
		return os;
	}
};

// ----------
int main()
// ----------
{
	std::array<Dog, 1000> dogs;

	for (Dog dog : dogs) {
		std::cout << dog << std::endl;
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-23 14:54:30.4720546
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 23일																					(3주 2일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O - class 객체
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"
#include <random>
#include <string>
#include <print>
#include <array>
#include <fstream>

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };
std::uniform_int_distribution uidNameLen{ 10, 30 };
std::uniform_int_distribution<int> uidChar{ 'a', 'z' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}

private:
	std::string name;		// [10, 30] 사이의 소문자로 구성된 이름
	size_t id;						// [0, 9999]


	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:>4}] - {}", dog.id, dog.name);
		return os;
	}
};

// ----------
int main()
// ----------
{
	std::array<Dog, 1000> dogs;

	std::ofstream out{ "Dog천마리", std::ios::binary };
	out.write((char*)dogs.data(), dogs.size() * sizeof(Dog));

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-24 16:22:22.1539205
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 24일																					(4주 1일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// [문제] 사용자가 입력한 수만큼 int를 저장할 메모리를 확보하라.
// 1부터 시작하는 정수로 메모리를 채워라.
// 합계를 화면에 출력하라.

// [메모]
// vector 사용했을 때 메모리 극한으로 사용하는 법
/*
- 메모리 낭비를 줄이는 STL의 전략
메모리를 딱 맞춰서 쓰고 싶다면 다음 두 가지 방법을 사용할 수 있습니다.

① reserve()로 미리 딱 맞게 예약하기
사용자가 입력한 count를 미리 알고 있다면, reserve(count)를 호출하여 딱 그만큼만 메모리를 할당하도록 유도할 수 있습니다.

② shrink_to_fit()으로 남는 공간 반납하기 (C++11 이상)
데이터를 다 채운 뒤, v.shrink_to_fit()을 호출하면 capacity를 현재 size에 맞춰 줄여줍니다. 즉, new로 할당한 것과 똑같은 메모리 상태를 만듭니다.
*/

// ----------
int main()
// ----------
{
    int count;
    std::cout << "숫자 몇까지 저장할 건지 입력하세요 : ";
    std::cin >> count;
    if (count < 0) {
        std::cout << "음수는 안돼요" << std::endl;
        return 20260324;
    }
    int* arr = new int[count];

    for (int i = 0; i < count; ++i) {
        arr[i] = i + 1;
    }

    int sum = 0;
    for (int i = 0; i < count; ++i) {
        sum += arr[i];
    }
    std::cout << sum;

    delete[]arr;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-24 16:39:59.7057883
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 24일																					(4주 1일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// [문제] 사용자가 입력한 수만큼 int를 저장할 메모리를 확보하라.
// 1부터 시작하는 정수로 메모리를 채워라.
// 합계를 화면에 출력하라.

// [메모]
// vector 사용했을 때 메모리 극한으로 사용하는 법
/*
- 메모리 낭비를 줄이는 STL의 전략
메모리를 딱 맞춰서 쓰고 싶다면 다음 두 가지 방법을 사용할 수 있습니다.

① reserve()로 미리 딱 맞게 예약하기
사용자가 입력한 count를 미리 알고 있다면, reserve(count)를 호출하여 딱 그만큼만 메모리를 할당하도록 유도할 수 있습니다.

② shrink_to_fit()으로 남는 공간 반납하기 (C++11 이상)
데이터를 다 채운 뒤, v.shrink_to_fit()을 호출하면 capacity를 현재 size에 맞춰 줄여줍니다. 즉, new로 할당한 것과 똑같은 메모리 상태를 만듭니다.
*/

// ----------
int main()
// ----------
{
	std::cout << "int 몇 개가 필요하신지? ";
	size_t num;
	std::cin >> num;

	// 설마 음수를 입력하지는 않으리라는 강력한 희망을!! (검사 안하겠다는 뜻)

	int* p;									// 스택에 들어가는 객체
	try {									// 만약 더 이상 할당할 수 있는 메모리가 없으면 뻗어버림. (기절) 그래서 일단 try 해보고 할당 안되면 catch로 감.
		p = new int[num];			// 시작 주소를 리턴하고 그 주소로부터 int * num크기만큼 메모리를 할당해줌!
	}
	catch (std::exception& e) {
		std::cout << "메모리 고갈 - " << e.what() << std::endl;
	}

	for (int i = 0; i < num; ++i) {
		p[i] = i + 1;
	}

	int sum{};
	for (int i = 0; i < num; ++i) {
		sum += p[i];
	}

	std::cout << "1부터 " << num << "까지의 합계 : " << sum << std::endl;
	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-30 13:40:38.5851899
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 24일																					(4주 1일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
// smart pointer -> callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"
#include <numeric>
#include <memory>

// [문제] 사용자가 입력한 수만큼 int를 저장할 메모리를 확보하라.
// 1부터 시작하는 정수로 메모리를 채워라.
// 합계를 화면에 출력하라.

// [메모]
// vector 사용했을 때 메모리 극한으로 사용하는 법
/*
- 메모리 낭비를 줄이는 STL의 전략
메모리를 딱 맞춰서 쓰고 싶다면 다음 두 가지 방법을 사용할 수 있습니다.

① reserve()로 미리 딱 맞게 예약하기
사용자가 입력한 count를 미리 알고 있다면, reserve(count)를 호출하여 딱 그만큼만 메모리를 할당하도록 유도할 수 있습니다.

② shrink_to_fit()으로 남는 공간 반납하기 (C++11 이상)
데이터를 다 채운 뒤, v.shrink_to_fit()을 호출하면 capacity를 현재 size에 맞춰 줄여줍니다. 즉, new로 할당한 것과 똑같은 메모리 상태를 만듭니다.
*/

// ----------
int main()
// ----------
{
	// c++ 옵티마이저 -> int* p를 자동으로 while 루프 밖으로 빼줌. (만들고 지우고 만들고 지우고 하면 알아서 밖으로 빼줌)
	// int* p;	스택에 들어가는 객체 -> 사용하지 말아야 함
	// C++11(Modern C++)에 이것의 완벽한 대체 수단이 있기 때문에 -> smart pointer

	std::unique_ptr<int[]> p;

	while (true) {
		std::cout << "int 몇 개가 필요하신지? ";
		size_t num;
		std::cin >> num;

		// 설마 음수를 입력하지는 않으리라는 강력한 희망을!! (검사 안하겠다는 뜻)

		try {									// 만약 더 이상 할당할 수 있는 메모리가 없으면 뻗어버림. (기절) 그래서 일단 try 해보고 할당 안되면 catch로 감.
			p.reset(new int[num]);			// 시작 주소를 리턴하고 그 주소로부터 int * num크기만큼 메모리를 할당해줌!
		}
		catch (std::exception& e) {
			std::cout << "메모리 고갈 - " << e.what() << std::endl;
		}

		//for (int i = 0; i < num; ++i) {
		//	p[i] = i + 1;
		//}
		std::iota(p.get(), p.get() + num, 1);			// p부터 p + num까지 1씩 증가하며 채우기

		//for (int i = 0; i < num; ++i) {
		//	sum += p[i];
		//}
		long long sum = std::accumulate(p.get(), p.get() + num, 0LL);		// p부터 p + num까지 더하기 (마지막 인자는 항등원)

		std::cout << "1부터 " << num << "까지의 합계 : " << sum << std::endl;
		save("메인.cpp");
	}
	
	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-30 13:42:23.6081257
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
// smart pointer -> callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// ----------
int main()
// ----------
{
	int* p;

	p = new int;

	std::cout << *p << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-30 13:42:41.2690052
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
// smart pointer -> callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// ----------
int main()
// ----------
{
	int* p;

	p = new int{20260330};

	std::cout << *p << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-30 13:44:12.0988658
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
// smart pointer -> callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// ----------
int main()
// ----------
{
	int* p;

	p = new int{20260330};

	std::cout << *p << std::endl;

	save("메인.cpp");

	delete p;
	delete p;
}

==========================================================================================
저장한 시간 - 2026-03-30 13:54:54.5865516
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
// smart pointer -> callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// C++ 언어의 보증사항
// 프로그램이 정상 종료시 (return 문, exit과는 다름) 스택에 생성된 지역객체를
// 반드시 소멸됨을 보증한다.

class Dog {
public:
	Dog() { std::cout << "생성" << std::endl; }
	~Dog() { std::cout << "소멸" << std::endl; }
};

// ----------
int main()
// ----------
{
	save("메인.cpp");

	Dog* p = new Dog;

	delete p;
}

==========================================================================================
저장한 시간 - 2026-03-30 13:55:34.9589920
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
// smart pointer -> callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// C++ 언어의 보증사항
// 프로그램이 정상 종료시 (return 문, exit과는 다름) 스택에 생성된 지역객체를
// 반드시 소멸됨을 보증한다.

class Dog {
public:
	Dog() { std::cout << "생성" << std::endl; }
	~Dog() { std::cout << "소멸" << std::endl; }
};

// ----------
int main()
// ----------
{
	save("메인.cpp");

	Dog* p = new Dog;

	return 0;	// 과연 소멸됨을 보증할까? 

	delete p;
}

==========================================================================================
저장한 시간 - 2026-03-30 13:59:48.6789303
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
// smart pointer -> callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <memory>
#include "save.h"

// C++ 언어의 보증사항
// 프로그램이 정상 종료시 (return 문, exit과는 다름) 스택에 생성된 지역객체를
// 반드시 소멸됨을 보증한다.

class Dog {
public:
	Dog() { std::cout << "생성" << std::endl; }
	~Dog() { std::cout << "소멸" << std::endl; }
};

// ----------
int main()
// ----------
{
	save("메인.cpp");

	// T* 는 raw-pointer이다. 앞으로 쓰지 말자

	// Dog* p = new Dog;
	std::unique_ptr<Dog> p{ new Dog };		// RAII

	return 0;	// 과연 소멸됨을 보증할까? -> 아닌데??

	std::cout << "이 문장이 출력되면 노트북을 버려라" << std::endl;
}

==========================================================================================
저장한 시간 - 2026-03-30 14:04:25.7176596
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
// smart pointer -> callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <memory>
#include "save.h"

// C++ 언어의 보증사항
// 프로그램이 정상 종료시 (return 문, exit과는 다름) 스택에 생성된 지역객체를
// 반드시 소멸됨을 보증한다.

class Dog {
public:
	Dog() { std::cout << "생성" << std::endl; }
	~Dog() { std::cout << "소멸" << std::endl; }
};

class 스마트포인터 {
private:
	Dog* p;
public:
	스마트포인터(Dog* p) : p{ p } {}
	~스마트포인터() {
		delete p;
	}
};

// ----------
int main()
// ----------
{
	save("메인.cpp");

	// T* 는 raw-pointer이다. 앞으로 쓰지 말자

	// Dog* p = new Dog;
	스마트포인터 p{ new Dog };		// RAII

	return 0;	// 과연 소멸됨을 보증할까? -> 아닌데??

	std::cout << "이 문장이 출력되면 노트북을 버려라" << std::endl;
}

==========================================================================================
저장한 시간 - 2026-03-30 14:11:09.2347151
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
// smart pointer -> callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <memory>
#include "save.h"

// C++ 언어의 보증사항
// 프로그램이 정상 종료시 (return 문, exit과는 다름) 스택에 생성된 지역객체를
// 반드시 소멸됨을 보증한다.
// stack-unwiding

class Dog {
public:
	Dog() { std::cout << "생성" << std::endl; }
	~Dog() { std::cout << "소멸" << std::endl; }
};


// ----------
int main()
// ----------
{
	std::unique_ptr<Dog[]> p {new Dog[10]};

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-30 14:30:46.4174093
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// [문제] "메인.cpp"의 소문자를 모두 대문자로 바꾼 "메인대문자.cpp"를 만들어라.

// ----------
int main()
// ----------
{


	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-30 14:49:19.8643717
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <algorithm>
#include "save.h"

// [문제] "메인.cpp"의 소문자를 모두 대문자로 바꾼 "메인대문자.cpp"를 만들어라.
char 소투대(char c) {
	return c = toupper(c);
}

// ----------
int main()
// ----------
{
	std::ifstream in{ "메인.cpp" };
	if (not in) {
		return 20260330;
	}

	std::ofstream out{ "메인대문자.cpp" };

	transform(std::istreambuf_iterator<char>{in}, {}, std::ostreambuf_iterator<char>{out}, 소투대);

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-30 14:51:21.7078449
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <algorithm>
#include "save.h"

// [문제] "메인.cpp"의 소문자를 모두 대문자로 바꾼 "메인대문자.cpp"를 만들어라.
char 소투대(char c) {
	return c = toupper(c);
}

// ----------
int main()
// ----------
{
	std::ifstream in{ "메인.cpp" };
	if (not in) {
		return 20260330;
	}

	std::ofstream out{ "메인대문자.cpp" };

	transform(std::istreambuf_iterator<char>{in}, {}, std::ostreambuf_iterator<char>{out}, 
		[](char c) {
		return c = toupper(c);
		});

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-30 15:15:23.0430209
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <array>
#include "save.h"

// [문제] 랜덤값을 갖는 int 1000만개를 메모리에 저장하라.
// qsort를 이용하여 오름차순 정렬하라.
// 정렬결과를 앞에서부터 1000개만 화면에 출력하라.

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };

std::array<int, 1000'0000> a;

int 오름차순(const void* a, const void* b) {
    return (*(int*)a - *(int*)b);
}

// ----------
int main()
// ----------
{
    save("메인.cpp");

    for (int& num : a) {
        num = uid(dre);
   }

    // 시간측정시작
    qsort(a.data(), a.size(), sizeof(std::array<int, 1000'0000>::value_type), 오름차순);
    // 측정 끝
	
    std::cout << "정렬 후 출력" << std::endl;
    for (int num : a) {
        std::print("{:8}", num);
    }
    std::cout << std::endl;
}

==========================================================================================
저장한 시간 - 2026-03-30 15:16:29.3946423
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <array>
#include "save.h"

// [문제] 랜덤값을 갖는 int 1000만개를 메모리에 저장하라.
// qsort를 이용하여 오름차순 정렬하라.
// 정렬결과를 앞에서부터 1000개만 화면에 출력하라.

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };

std::array<int, 1000'0000> a;

int 오름차순(const void* a, const void* b) {
    return (*(int*)a - *(int*)b);
}

// ----------
int main()
// ----------
{
    save("메인.cpp");

    for (int& num : a) {
        num = uid(dre);
   }

    // 시간측정시작
    qsort(a.data(), a.size(), sizeof(std::array<int, 1000'0000>::value_type), 오름차순);
    // 측정 끝
	
    std::cout << "정렬 후 출력" << std::endl;
    //for (int num : a) {
    //    std::print("{:8}", num);
    //}
    for (int i = 0; i < 1000; ++i) {
        std::print("{:8}", a[i]);
    }
    std::cout << std::endl;
}

==========================================================================================
저장한 시간 - 2026-03-30 15:19:39.1887091
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <array>
#include <ranges>
#include "save.h"

// [문제] 랜덤값을 갖는 int 1000만개를 메모리에 저장하라.
// qsort를 이용하여 오름차순 정렬하라.
// 정렬결과를 앞에서부터 1000개만 화면에 출력하라.

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };

std::array<int, 1000'0000> a;

int 오름차순(const void* a, const void* b) {
    return (*(int*)a - *(int*)b);
}

// ----------
int main()
// ----------
{
    save("메인.cpp");

    for (int& num : a) {
        num = uid(dre);
   }

    // 시간측정시작
    qsort(a.data(), a.size(), sizeof(std::array<int, 1000'0000>::value_type), 오름차순);
    // 측정 끝
	
    std::cout << "정렬 후 출력" << std::endl;
    for (int num : a | std::views::take(1000)) {      // | : 버티컬 바 (짝대기) -> 필터 역할 (데이터를 원하는 대로 뽑아줄 수 있음!!)
        std::print("{:8}", num);
    }
    //for (int i = 0; i < 1000; ++i) {
    //    std::print("{:8}", a[i]);
    //}
    std::cout << std::endl;
}

==========================================================================================
저장한 시간 - 2026-03-30 15:20:33.2716931
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <array>
#include <ranges>
#include "save.h"

// [문제] 랜덤값을 갖는 int 1000만개를 메모리에 저장하라.
// qsort를 이용하여 오름차순 정렬하라.
// 정렬결과를 앞에서부터 1000개만 화면에 출력하라.

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };

std::array<int, 1000'0000> a;

int 오름차순(const void* a, const void* b) {
    return (*(int*)a - *(int*)b);
}

// ----------
int main()
// ----------
{
    save("메인.cpp");

    for (int& num : a) {
        num = uid(dre);
   }

    // 시간측정시작
    qsort(a.data(), a.size(), sizeof(std::array<int, 1000'0000>::value_type), 오름차순);
    // 측정 끝
	
    std::cout << "정렬 후 출력" << std::endl;

    for (int num : a | std::views::reverse
                                | std::views::take(1000)) {      // | : 버티컬 바 (짝대기) -> 필터 역할 (데이터를 원하는 대로 뽑아줄 수 있음!!)
        std::print("{:8}", num);
    }

    std::cout << std::endl;
}