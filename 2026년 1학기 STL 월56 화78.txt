//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 3일																					(1주 1일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.24 (17.14.27로 맞추기!)
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

int main()
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자

void save(const std::string& fname)
{
	std::ifstream in{ fname };
	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };

	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostream_iterator<char>{out});
}//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 3일																					(1주 1일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	
	
	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };

	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostream_iterator<char>{out});
}//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 3일																					(1주 1일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 9일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	
	
	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };

	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostream_iterator<char>{out});
}//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 3일																					(1주 1일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 9일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	
	
	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };

	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostream_iterator<char>{out});
}//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 3일																					(1주 1일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 9일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	
	
	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };
	// if (not out)		이건 할 필요 없음. std::ios::app -> 옛날 파일 그대로 두고 덮어쓰는 모드

	//std::copy(std::istreambuf_iterator<char>{in}, {},
	//	std::ostream_iterator<char>{out});

	char c;
	in >> std::noskipws;		// 이렇게 쓰면 공백 포함
	while(in >> c)		// operator>>(in, c); 이렇게 바뀜. (연산자 오버로딩)
		out << c;
}//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 3일																					(1주 1일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 9일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	
	
	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };
	// if (not out)		이건 할 필요 없음. std::ios::app -> 옛날 파일 그대로 두고 덮어쓰는 모드

	//std::copy(std::istreambuf_iterator<char>{in}, {},
	//	std::ostream_iterator<char>{out});

	char c;
	in >> std::noskipws;		// 이렇게 쓰면 공백 포함
	while(in >> c)		// operator>>(in, c); 이렇게 바뀜. (연산자 오버로딩)
		out << c;		// 파일에 직접 쓰기
}

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 9일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	
	
	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };
	// if (not out)		이건 할 필요 없음. std::ios::app -> 옛날 파일 그대로 두고 덮어쓰는 모드

	// std::copy( 어디부터, 어디까지를, 어디로 );

	std::copy(std::istreambuf_iterator<char>{in}, {},		// iterator in파일로부터 char, {} -> EOF, 마지막 글자까지 (특별한 약속)
		std::ostream_iterator<char>{out});

	//char c;
	//in >> std::noskipws;		// 이렇게 쓰면 공백 포함
	//while(in >> c)		// operator>>(in, c); 이렇게 바뀜. (연산자 오버로딩)
	//	out << c;		// 파일에 직접 쓰기
}//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 9일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	std::cout << "\"" << fname << "\"를 저장합니다" << '\n';


	
	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };
	// if (not out)		이건 할 필요 없음. std::ios::app -> 옛날 파일 그대로 두고 덮어쓰는 모드

	// std::copy( 어디부터, 어디까지를, 어디로 );
	// buf없으면 글자'만' 읽어옴. buf 붙이면 화면에 있는 내용 그대로 옮기라는 뜻.
	std::copy(std::istreambuf_iterator<char>{in}, {},		// iterator in파일로부터 char, {} -> EOF, 마지막 글자까지 (특별한 약속)
		std::ostream_iterator<char>{out});

	//char c;
	//in >> std::noskipws;		// 이렇게 쓰면 공백 포함
	//while(in >> c)		// operator>>(in, c); 이렇게 바뀜. (연산자 오버로딩)
	//	out << c;		// 파일에 직접 쓰기
}

==========================================================================================
저장한 시간 - 
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 9일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	std::cout << "\"" << fname << "\"를 저장합니다" << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };
	// if (not out)		이건 할 필요 없음. std::ios::app -> 옛날 파일 그대로 두고 덮어쓰는 모드

	out << '\n' << '\n';
	out << "==========================================================================================" << '\n';
	out << "저장한 시간 - " << '\n';
	out << "==========================================================================================" << '\n';
	out << '\n';

	// std::copy( 어디부터, 어디까지를, 어디로 );
	// buf없으면 글자'만' 읽어옴. buf 붙이면 화면에 있는 내용 그대로 옮기라는 뜻.
	std::copy(std::istreambuf_iterator<char>{in}, {},		// iterator in파일로부터 char, {} -> EOF, 마지막 글자까지 (특별한 약속)
		std::ostream_iterator<char>{out});

	//char c;
	//in >> std::noskipws;		// 이렇게 쓰면 공백 포함
	//while(in >> c)		// operator>>(in, c); 이렇게 바뀜. (연산자 오버로딩)
	//	out << c;		// 파일에 직접 쓰기
}

==========================================================================================
저장한 시간 - 
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 9일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	std::cout << "\"" << fname << "\"를 저장합니다" << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };
	// if (not out)		이건 할 필요 없음. std::ios::app -> 옛날 파일 그대로 두고 덮어쓰는 모드

	// 시간정보를 얻는다
	auto now = std::chrono::system_clock::now();		// time epoch로 부터 1초마다 증가한 tick 수
	// 데이터 타입은 컴파일러가 추측할 수 있기 때문에(마우스 갖다대면 알려줌) 직접 써줘도 되고 auto를 써서 컴파일러가 자동으로 써주게끔 할 수 있음.

	std::cout << now << '\n';

	out << '\n' << '\n';
	out << "==========================================================================================" << '\n';
	out << "저장한 시간 - " << '\n';
	out << "==========================================================================================" << '\n';
	out << '\n';

	// std::copy( 어디부터, 어디까지를, 어디로 );
	// buf없으면 글자'만' 읽어옴. buf 붙이면 화면에 있는 내용 그대로 옮기라는 뜻.
	std::copy(std::istreambuf_iterator<char>{in}, {},		// iterator in파일로부터 char, {} -> EOF, 마지막 글자까지 (특별한 약속)
		std::ostream_iterator<char>{out});

	//char c;
	//in >> std::noskipws;		// 이렇게 쓰면 공백 포함
	//while(in >> c)		// operator>>(in, c); 이렇게 바뀜. (연산자 오버로딩)
	//	out << c;		// 파일에 직접 쓰기
}

==========================================================================================
저장한 시간 - 
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 9일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	std::cout << "\"" << fname << "\"를 저장합니다" << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };
	// if (not out)		이건 할 필요 없음. std::ios::app -> 옛날 파일 그대로 두고 덮어쓰는 모드

	// 시간정보를 얻는다
	auto now = std::chrono::system_clock::now();		// time epoch로 부터 1초마다 증가한 tick 수
	// 데이터 타입은 컴파일러가 추측할 수 있기 때문에(마우스 갖다대면 알려줌) 직접 써줘도 되고 auto를 써서 컴파일러가 자동으로 써주게끔 할 수 있음.

	std::cout << now << '\n';

	out << '\n' << '\n';
	out << "==========================================================================================" << '\n';
	out << "저장한 시간 - " << '\n';
	out << "==========================================================================================" << '\n';
	out << '\n';

	// std::copy( 어디부터, 어디까지를, 어디로 );
	// buf없으면 글자'만' 읽어옴. buf 붙이면 화면에 있는 내용 그대로 옮기라는 뜻.
	std::copy(std::istreambuf_iterator<char>{in}, {},		// iterator in파일로부터 char, {} -> EOF, 마지막 글자까지 (특별한 약속)
		std::ostream_iterator<char>{out});

	//char c;
	//in >> std::noskipws;		// 이렇게 쓰면 공백 포함
	//while(in >> c)		// operator>>(in, c); 이렇게 바뀜. (연산자 오버로딩)
	//	out << c;		// 파일에 직접 쓰기
}

시간형식 / 통화형식

==========================================================================================
저장한 시간 - 2026-03-09 14:22:00.4361075
저장한 시간 - 2026년 3월 9일 오후 2:22 로 바꿔보기.
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "2026년 3월 9일" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌. 
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

// 여기 설명부터 다시 합니다.
// fname부터 하자 -> naming convention

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not	in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}
	
	std::cout << "\"" << fname << "\"를 저장합니다" << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };
	// if (not out)		이건 할 필요 없음. std::ios::app -> 옛날 파일 그대로 두고 덮어쓰는 모드

	// 시간정보를 얻는다
	auto now = std::chrono::system_clock::now();		// time epoch로 부터 1초마다 증가한 tick 수
	// 데이터 타입은 컴파일러가 추측할 수 있기 때문에(마우스 갖다대면 알려줌) 직접 써줘도 되고 auto를 써서 컴파일러가 자동으로 써주게끔 할 수 있음.

	using namespace std::chrono_literals;
	// out << now + 9h << '\n';		// 여기서 now는 그리니치 천문대 기준 시간 (우리나라로 바꾸려면 +9시간 해줘야함)

	out << '\n' << '\n';
	out << "==========================================================================================" << '\n';
	out << "저장한 시간 - " << now + 9h << '\n';
	out << "==========================================================================================" << '\n';
	out << '\n';

	// std::copy( 어디부터, 어디까지를, 어디로 );
	// buf없으면 글자'만' 읽어옴. buf 붙이면 화면에 있는 내용 그대로 옮기라는 뜻.
	std::copy(std::istreambuf_iterator<char>{in}, {},		// iterator in파일로부터 char, {} -> EOF, 마지막 글자까지 (특별한 약속)
		std::ostream_iterator<char>{out});

	//char c;
	//in >> std::noskipws;		// 이렇게 쓰면 공백 포함
	//while(in >> c)		// operator>>(in, c); 이렇게 바뀜. (연산자 오버로딩)
	//	out << c;		// 파일에 직접 쓰기
}

==========================================================================================
저장한 시간 - 2026-03-09 14:45:37.2371114
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
//	컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++ 언어표준 - / std:c++latest
//						- C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------------------------------------------
// save 파일 분리
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "save를 파일로 분리하였다" << '\n';
	
	save("메인.cpp");
	// return 0;		// 이건 표준이 아님 (없어야 됨) -> 내가 안써도 컴파일러가 자동으로 넣어줌.
	// return 20260303;		// 이건 가능 (필요하다면 써도 되지만 return 0를 쓰는 건 비표준임)
}

==========================================================================================
저장한 시간 - 2026-03-09 14:52:14.1940674
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
// save 파일 분리
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "save를 파일로 분리하였다" << '\n';
	
	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-09 14:52:50.1417031
==========================================================================================

// -------------------------------------------------------------------------------------------------
// 한 학기 강의를 저장하기 위한 함수
// 
// 2026. 03. 09 (월)																																					백성윤
// -------------------------------------------------------------------------------------------------
#pragma once

void save(const std::string&);		// 선언문 (얼마든지 해도 상관없음 백만번 가능)

==========================================================================================
저장한 시간 - 2026-03-09 14:52:50.1517691
==========================================================================================

// -------------------------------------------------------------------------------------------------
// 한 학기 강의를 저장하기 위한 함수
// 
// 2026. 03. 09 (월)																																					백성윤
// -------------------------------------------------------------------------------------------------
// fname부터 하자 -> naming convention
#include <iostream>
#include <string>
#include <fstream>
#include <chrono>
// 헤더파일은 맨 나중에 넣기. 오류날 수 있음 (찾기 힘듦)
#include "save.h"

void save(const std::string& fname)
{
	std::ifstream in{ fname };		// RAII - 찾아보고 공부해보기
	// in.close();		// 절대 코딩 할 필요가 없다. 이걸 쓰면 나는 RAII에 대해 잘 모르는 바보라고 알려주는 꼴 (다 자동화 됨)
	if (not in) {		// 느낌표 대신 not 쓰기
		std::cout << fname << " - 열 수 없습니다" << '\n';
		// return 20260309;		// return은 함수를 호출한 곳으로 빠져나가는 코드
		exit(20260309);
	}

	std::cout << "\"" << fname << "\"를 저장합니다" << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56 화78.txt", std::ios::app };
	// if (not out)		이건 할 필요 없음. std::ios::app -> 옛날 파일 그대로 두고 덮어쓰는 모드

	// 시간정보를 얻는다
	auto now = std::chrono::system_clock::now();		// time epoch로 부터 1초마다 증가한 tick 수
	// 데이터 타입은 컴파일러가 추측할 수 있기 때문에(마우스 갖다대면 알려줌) 직접 써줘도 되고 auto를 써서 컴파일러가 자동으로 써주게끔 할 수 있음.

	using namespace std::chrono_literals;
	// out << now + 9h << '\n';		// 여기서 now는 그리니치 천문대 기준 시간 (우리나라로 바꾸려면 +9시간 해줘야함)

	out << '\n' << '\n';
	out << "==========================================================================================" << '\n';
	out << "저장한 시간 - " << now + 9h << '\n';
	out << "==========================================================================================" << '\n';
	out << '\n';

	// std::copy( 어디부터, 어디까지를, 어디로 );
	// buf없으면 글자'만' 읽어옴. buf 붙이면 화면에 있는 내용 그대로 옮기라는 뜻.
	std::copy(std::istreambuf_iterator<char>{in}, {},		// iterator in파일로부터 char, {} -> EOF, 마지막 글자까지 (특별한 약속)
		std::ostream_iterator<char>{out});

	//char c;
	//in >> std::noskipws;		// 이렇게 쓰면 공백 포함
	//while(in >> c)		// operator>>(in, c); 이렇게 바뀜. (연산자 오버로딩)
	//	out << c;		// 파일에 직접 쓰기
}

==========================================================================================
저장한 시간 - 2026-03-09 14:52:50.1615429
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
// save 파일 분리
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	std::cout << "save를 파일로 분리하였다" << '\n';
	
	save("save.h");
	save("save.cpp");
	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-09 14:55:15.8959653
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"
// using namespace std;		// 강의 시 가독성을 위해 사용, 나는 이렇게 코딩하지 말자

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-09 14:56:39.6176200
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

// ----------
int main()
// ----------
{
	// [문제] 의도대로 실행되게 하자
	int a{ 1 }, b{ 2 };

	// 여기에서 change를 호출하였다.

	std::cout << a << ", " << b << std::endl;		// 2, 1이라고 출력되어야 함

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-09 14:58:09.1715802
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

void change(int &a, int &b);

// ----------
int main()
// ----------
{
	// [문제] 의도대로 실행되게 하자
	int a{ 1 }, b{ 2 };

	// 여기에서 change를 호출하였다.
	change(a, b);

	std::cout << a << ", " << b << std::endl;		// 2, 1이라고 출력되어야 함

	save("메인.cpp");
}

void change(int &a, int &b) {
	int temp = a;
	a = b;
	b = temp;
}

==========================================================================================
저장한 시간 - 2026-03-09 15:06:57.9356357
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

void change(int &a, int &b);

// ----------
int main()
// ----------
{
	// 얘네는 코드 세그먼트
	std::cout << "main - " << std::addressof(main) << std::endl;
	std::cout << "save - " << std::addressof(save) << std::endl;

	// [문제] 의도대로 실행되게 하자
	int a{ 1 }, b{ 2 };

	// 얘네는 스택
	std::cout << "a - " << std::addressof(a) << std::endl;
	std::cout << "b - " << std::addressof(b) << std::endl;

	// 여기에서 change를 호출하였다.
	// change란? 호출할 수 있는 어떤 것. (호출할 만한게 무한대임) -> 호출 연산자는 ()
	change(a, b);

	std::cout << a << ", " << b << std::endl;		// 2, 1이라고 출력되어야 함

	save("메인.cpp");
}

void change(int &a, int &b) {
	int temp = a;
	a = b;
	b = temp;
}

==========================================================================================
저장한 시간 - 2026-03-09 15:08:19.4433843
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

void change(int &a, int &b);

int a{ 1 };

// ----------
int main()
// ----------
{
	// 얘네는 코드 세그먼트
	std::cout << "main - " << std::addressof(main) << std::endl;
	std::cout << "save - " << std::addressof(save) << std::endl;

	// [문제] 의도대로 실행되게 하자
	int b{ 2 };

	// 얘네는 스택
	std::cout << "a - " << std::addressof(a) << std::endl;		// 전역으로 옮기면 코드 세그먼트로 바뀜
	std::cout << "b - " << std::addressof(b) << std::endl;

	// 여기에서 change를 호출하였다.
	// change란? 호출할 수 있는 어떤 것. (호출할 만한게 무한대임) -> 호출 연산자는 ()
	change(a, b);

	std::cout << a << ", " << b << std::endl;		// 2, 1이라고 출력되어야 함

	save("메인.cpp");
}

void change(int &a, int &b) {
	int temp = a;
	a = b;
	b = temp;
}

==========================================================================================
저장한 시간 - 2026-03-09 15:20:01.4896362
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

void change(int *, int *);

// ----------
int main()
// ----------
{
	// 얘네는 코드 세그먼트
	//std::cout << "main - " << std::addressof(main) << std::endl;
	//std::cout << "save - " << std::addressof(save) << std::endl;
	{
		// [문제] 의도대로 실행되게 하자
		// Dog는 class로 작성하자.
		using Dog = int;		// #define Dog int 이건 사용하면 안된다
		Dog a{ 1 }, b{ 2 };

		// 얘네는 스택
		//std::cout << "a - " << std::addressof(a) << std::endl;		// 전역으로 옮기면 main, save의 윗동네로 변경
		//std::cout << "b - " << std::addressof(b) << std::endl;

		// 여기에서 change를 호출하였다.
		// change란? 호출할 수 있는 어떤 것. (호출할 만한게 무한대임) -> 호출 연산자는 ()
		// change(a, b); -> 이것도 맞지만 돌아가는 답임. 컴파일러가 알아서 주소로 변환해서 돌아가게 해줌. (항상 교수님이 알려준 코드처럼 코딩하자 포인터로)
		// 레퍼런스는 실제로 존재하지 않는다!!!!!! (찾아보기)
		change(&a, &b);

		std::cout << a << ", " << b << std::endl;		// 2, 1이라고 출력되어야 함
	}

	{
		// [문제] 의도대로 실행되게 하자
		int a{ 1 }, b{ 2 };

		// 얘네는 스택
		//std::cout << "a - " << std::addressof(a) << std::endl;		// 전역으로 옮기면 main, save의 윗동네로 변경
		//std::cout << "b - " << std::addressof(b) << std::endl;

		// 여기에서 change를 호출하였다.
		// change란? 호출할 수 있는 어떤 것. (호출할 만한게 무한대임) -> 호출 연산자는 ()
		// change(a, b); -> 이것도 맞지만 돌아가는 답임. 컴파일러가 알아서 주소로 변환해서 돌아가게 해줌. (항상 교수님이 알려준 코드처럼 코딩하자 포인터로)
		// 레퍼런스는 실제로 존재하지 않는다!!!!!! (찾아보기)
		change(&a, &b);

		std::cout << a << ", " << b << std::endl;		// 2, 1이라고 출력되어야 함
	}

	save("메인.cpp");
}

void change(int *p, int *q) {
	int temp{ *p };
	*p = *q;
	*q = temp;
}

==========================================================================================
저장한 시간 - 2026-03-10 15:52:26.8049560
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 9일																					(1주 2일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// 사용하지 말아야 할 자료형 - 대체할 수 있다 -> zero-overhead (불이익이 하나도 없음)
// 1. char*		-> std::string
// 2. T*			-> smart pointer를 사용할 것
// 3. T[]		-> std::array를 사용할 것

class Dog;			// forward declaration

void change(int *, int *);

class Dog {		// 중괄호는 여기에 쓰자! (좋은 코드 습관)
private:
	int num;

public:
	Dog() = default;		// 디폴트 생성자 만들어주기!
	explicit Dog(int n) : num{ n } { }
	// ~Dog() 쓸데없이 소멸자 만들지말기 (0점짜리)

	// 이 코드는 사용하지 말자
	//operator int () {
	//	return num;
	//}


	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		return os << dog.num;
	}
};

void change(Dog& p, Dog& q) {
	Dog temp{ p };		// temp를 "복사 생성"하라	- 내가 안만들어도 돌아가는 이유 => 스페셜 함수이기 때문! 컴파일러가 알아서 만들어줌
	p = q;		// p.operator = (q); -> 스페셜 함수!! 자동으로 만들어서 돌아가게 해줌
	q = temp;
}

// ----------
int main()
// ----------
{
	// 얘네는 코드 세그먼트
	//std::cout << "main - " << std::addressof(main) << std::endl;
	//std::cout << "save - " << std::addressof(save) << std::endl;
	{
		// [문제] 의도대로 실행되게 하자
		// Dog는 class로 작성하자.
		// using Dog = int;		// #define Dog int 이건 사용하면 안된다
		Dog a{ 1 }, b{ 2 };		// Dog a = 1, b = 2; 이런식으로 쓰는 건 좀 "위험" 하다
		// Dog c = 1;		// 이렇게 사용하면 내가 원치않게 실행될 수 있음. 컴파일러가 알아서 형변환 실행 (그래서 explicit 붙이면 바로 오류)

		// 얘네는 스택
		//std::cout << "a - " << std::addressof(a) << std::endl;		// 전역으로 옮기면 main, save의 윗동네로 변경
		//std::cout << "b - " << std::addressof(b) << std::endl;

		// 여기에서 change를 호출하였다.
		// change란? 호출할 수 있는 어떤 것. (호출할 만한게 무한대임) -> 호출 연산자는 ()
		// change(a, b); -> 이것도 맞지만 돌아가는 답임. 컴파일러가 알아서 주소로 변환해서 돌아가게 해줌. (항상 교수님이 알려준 코드처럼 코딩하자 포인터로)
		// 레퍼런스는 실제로 존재하지 않는다!!!!!! (찾아보기)
		change(a, b);

		std::cout << a << ", " << b << std::endl;		// 2, 1이라고 출력되어야 함
	}

	{
		// [문제] 의도대로 실행되게 하자
		int a{ 1 }, b{ 2 };

		// 얘네는 스택
		//std::cout << "a - " << std::addressof(a) << std::endl;		// 전역으로 옮기면 main, save의 윗동네로 변경
		//std::cout << "b - " << std::addressof(b) << std::endl;

		// 여기에서 change를 호출하였다.
		// change란? 호출할 수 있는 어떤 것. (호출할 만한게 무한대임) -> 호출 연산자는 ()
		// change(a, b); -> 이것도 맞지만 돌아가는 답임. 컴파일러가 알아서 주소로 변환해서 돌아가게 해줌. (항상 교수님이 알려준 코드처럼 코딩하자 포인터로)
		// 레퍼런스는 실제로 존재하지 않는다!!!!!! (찾아보기)
		change(&a, &b);

		std::cout << a << ", " << b << std::endl;		// 2, 1이라고 출력되어야 함
	}

	save("메인.cpp");
}

void change(int *p, int *q) {
	int temp{ *p };
	*p = *q;
	*q = temp;
}

==========================================================================================
저장한 시간 - 2026-03-10 16:02:40.8034691
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

void change(std::string);
void change(int, int) {
	std::cout << "int 2개를 인자로" << std::endl;
}

// ----------
int main()
// ----------
{
	change(1, 2);

	save("메인.cpp");
}


==========================================================================================
저장한 시간 - 2026-03-10 16:04:30.2802691
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

void change(std::string);

void change(int, int) {
	std::cout << "int 2개를 인자로" << std::endl;
}

// ----------
int main()
// ----------
{
	change(1, 2);
	//change("12345 ");		// 돌려보면 컴파일은 되는데 링크 단계에서 오류 발생 (change@@YAXC ~~ 라는 이름의 함수를 지가 알아서 만듦!)

	save("메인.cpp");
}


==========================================================================================
저장한 시간 - 2026-03-10 16:46:39.5635830
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없다!
template<class T>
void change(T& p, T& q) {									// 자동화
	T temp{ p };				
	p = q;
	q = temp;
}

// double에 대한 sepcialization
template<>
void change<double>(double& p, double& q) {									// 자동화
	std::cout << "컴파일러가 찍어낸 코드" << std::endl;
	double temp{ p };
	p = q;
	q = temp;
}

// ----------
int main()
// ----------
{
	double a, b;
	change<double>(a, b);							// 뭘 호출해야 하나? - 호출우선순위 결정

	// change("S", "T");

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-10 16:52:58.5949239
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없다!
template<class T>
void change(T& p, T& q) {									// 자동화
	T temp{ p };				
	p = q;
	q = temp;
}

// double에 대한 sepcialization
template<>
void change<double>(double& p, double& q) {									// 자동화
	std::cout << "컴파일러가 찍어낸 코드" << std::endl;
	double temp{ p };
	p = q;
	q = temp;
}

// ----------
int main()
// ----------
{
	double a, b;
	change<double>(a, b);							// 뭘 호출해야 하나? - 호출우선순위 결정

	std::string s{ "2026년" }, t{ "3월10일" };
	change(s, t);

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-10 16:53:48.6271206
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없다!
template<class T>
void change(T& p, T& q) {									// 자동화
	std::cout << "이 문장을 보면 컴파일러가 열일한줄 알아라" << '\n';
	T temp{ p };				
	p = q;
	q = temp;
}

// double에 대한 sepcialization
template<>
void change<double>(double& p, double& q) {									// 자동화
	std::cout << "컴파일러가 찍어낸 코드" << std::endl;
	double temp{ p };
	p = q;
	q = temp;
}

// ----------
int main()
// ----------
{
	double a, b;
	change<double>(a, b);							// 뭘 호출해야 하나? - 호출우선순위 결정

	std::string s{ "2026년" }, t{ "3월10일" };
	change(s, t);

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-10 16:54:29.0132084
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없다!
template<class T>
void change(T& p, T& q) {									// 자동화
	std::cout << "이 문장을 보면 컴파일러가 열일한줄 알아라" << '\n';
	T temp{ p };				
	p = q;
	q = temp;
}

// double에 대한 sepcialization
//template<>
//void change<double>(double& p, double& q) {									// 자동화
//	std::cout << "컴파일러가 찍어낸 코드" << std::endl;
//	double temp{ p };
//	p = q;
//	q = temp;
//}

// ----------
int main()
// ----------
{
	double a, b;
	change<double>(a, b);							// 뭘 호출해야 하나? - 호출우선순위 결정

	std::string s{ "2026년" }, t{ "3월10일" };
	change(s, t);

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-10 17:06:52.7982696
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

template<class T>
T add(T a, T b) {
	return a + b;
}

// ----------
int main()
// ----------
{
	// 템플릿 함수 add를 한 개 만들어 실행되게 하라.
	std::cout << add<int>(1, 2) << std::endl;

	// std::cout << add( "2026년"s , "3월10일"s ) << std::endl;

	std::cout << add<std::string>(std::string{ "2026년" }, std::string{ "3월10일" }) << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-10 17:07:28.6019714
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// template 
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

template<class T>
T add(T a, T b) {
	return a + b;
}

// ----------
int main()
// ----------
{
	// 템플릿 함수 add를 한 개 만들어 실행되게 하라.
	std::cout << add<int>(1, 2) << std::endl;

	// std::cout << add( "2026년"s , "3월10일"s ) << std::endl;

	std::cout << add<std::string>(std::string{ "2026년 " }, std::string{ "3월 10일" }) << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:38:42.4965294
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std:: uniform_int_distribution uid;

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	for (int i = 0; i < 100; ++i) {
		std::cout << uid(dre) << std::endl;

	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:39:54.7362718
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, std::numeric_limits<int>::max()};

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	for (int i = 0; i < 100; ++i) {
		std::cout << uid(dre) << std::endl;

	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:40:41.6336575
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ std::numeric_limits<int>::min(), std::numeric_limits<int>::max()};

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	for (int i = 0; i < 100; ++i) {
		std::cout << uid(dre) << std::endl;

	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:41:14.5359003
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	for (int i = 0; i < 1000; ++i) {
		std::cout << uid(dre) << std::endl;

	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:41:32.2813724
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	for (int i = 0; i < 1000; ++i) {
		std::cout << uid(dre) << " ";

	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:42:52.5843581
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	for (int i = 0; i < 1000; ++i) {
		printf("{:8}", uid(dre));
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:43:15.5185315
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	for (int i = 0; i < 1000; ++i) {
		std::print("{:8}", uid(dre));
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:43:36.1036368
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	for (int i = 0; i < 1000; ++i) {
		std::print("{:8}", uid(dre));
	}
	std::cout << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:43:51.2068746
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	for (int i = 0; i < 1000; ++i) {
		std::print("{:<8}", uid(dre));
	}
	std::cout << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:48:38.8858554
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:50:49.4005227
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하시오.
	int max = -1;
	for (int i = 0; i < 1000; ++i) {
		if (max < a[i]) max = a[i];
	}

	std::cout << "가장 큰 값: " << max << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:50:55.4798215
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하시오.
	int max = -1;
	for (int i = 0; i < 1000; ++i) {
		if (max < a[i]) max = a[i];
	}

	std::cout << "가장 큰 값: " << max << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:50:57.6154344
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하시오.
	int max = -1;
	for (int i = 0; i < 1000; ++i) {
		if (max < a[i]) max = a[i];
	}

	std::cout << "가장 큰 값: " << max << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:51:00.4207794
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하시오.
	int max = -1;
	for (int i = 0; i < 1000; ++i) {
		if (max < a[i]) max = a[i];
	}

	std::cout << "가장 큰 값: " << max << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:53:43.7182732
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하시오.
	int max = -1;
	for (int num : a) {
		if (max < num) max = num;
	}

	std::cout << "가장 큰 값: " << max << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:55:10.4213197
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하시오.
	int max = -1;
	for (const int& num : a) {
		if (max < num) max = num;
	}

	std::cout << "가장 큰 값: " << max << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 13:55:37.4034269
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000]{ uid(dre) };

	//for (int& num : a) {
	//	num = uid(dre);
	//}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하시오.
	int max = -1;
	for (const int& num : a) {
		if (max < num) max = num;
	}

	std::cout << "가장 큰 값: " << max << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 14:02:06.4748435
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하시오.
	int maxVal{ std::numeric_limits<int>::min() };

	for (int num : a) {
		if (maxVal < num) maxVal = num;
	}

	std::cout << "가장 큰 값: " << maxVal<< std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 14:03:29.9138231
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하시오.
	std::sort(std::begin(a), std::end(a));
	std::cout << "가장 큰 값 - " << a[1000 - 1] << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 14:04:27.8766515
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하라.
	// 이런 답은 0점. 쓸데없는 일을 너무 많이 함. (컴퓨터 학대)
	std::sort(std::begin(a), std::end(a));
	std::cout << "가장 큰 값 - " << a[1000 - 1] << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 14:15:48.1266414
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하라.
	std::max_element(std::begin(a), std::end(a));

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 14:19:34.7408900
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하라.
	int* p = std::max_element( &a[0], &a[1000]);
	std::cout << "가장 큰 값은: " << *p << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 14:21:07.0598259
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하라.
	int* p = std::max_element( &a[0], &a[1000]);		// 납땜 코드 (hard-wired code)
	std::cout << "가장 큰 값은: " << *p << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 14:24:38.9226761
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//-------------------------------------------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드!
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하라.
	int* p = std::max_element( std::begin(a), std::end(a));
	std::cout << "가장 큰 값은: " << *p << std::endl;
	// 알고리즘을 이용하는 게 실수할 부분이 훨씬 적고, 일단 max 알고리즘은 병렬처리가 가능한 게 큼.

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 14:40:59.2598444
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//------------------------------------------------------------------------------------------------------- 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include "save.h"

// random_device - 마우스의 위치, cpu의 온도, 팬이 돌아가는 속도 등등을 고려 (완전한 랜덤)
std::default_random_engine dre{ std::random_device{}()};
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아서 화면에 출력하라.
	int* p = std::max_element( std::begin(a), std::end(a));
	 std::cout << "가장 큰 값은: " << *p << std::endl;
	// 알고리즘을 이용하는 게 실수할 부분이 훨씬 적고, 일단 max 알고리즘은 병렬처리가 가능한 게 큼.

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 14:46:30.0026174
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//------------------------------------------------------------------------------------------------------- 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 작은 값과 큰 값을 찾아서 화면에 출력하라.
	int* min_value = std::min_element(std::begin(a), std::end(a));
	int* max_value = std::max_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값: " << *min_value << std::endl;
	std::cout << "가장 큰 값: " << *max_value << std::endl;


	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 14:57:40.6946809
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//------------------------------------------------------------------------------------------------------- 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 작은 값과 큰 값을 찾아서 화면에 출력하라.
	std::pair<int*, int*>result = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값: " << *(result.first) << ", 가장 큰 값: " << *(result.second) << std::endl;
	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 15:01:23.1667366
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//------------------------------------------------------------------------------------------------------- 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 작은 값과 큰 값을 찾아서 화면에 출력하라.
	// syntatic sugar 인 structured binding을 사용하면 pair를 이렇게 쓸 수 있다!
	auto[작은값, 큰값] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값: " << 작은값 << ", 가장 큰 값: " << 큰값 << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 15:01:38.0581129
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//------------------------------------------------------------------------------------------------------- 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 작은 값과 큰 값을 찾아서 화면에 출력하라.
	// syntatic sugar 인 structured binding을 사용하면 pair를 이렇게 쓸 수 있다!
	auto[작은값, 큰값] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값: " << *(작은값) << ", 가장 큰 값: " << *(큰값) << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 15:01:51.9729281
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//------------------------------------------------------------------------------------------------------- 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 작은 값과 큰 값을 찾아서 화면에 출력하라.
	// syntatic sugar 인 structured binding을 사용하면 pair를 이렇게 쓸 수 있다!
	auto[작은값, 큰값] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값: " << *작은값 << ", 가장 큰 값: " << *큰값 << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 15:08:22.0218464
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//------------------------------------------------------------------------------------------------------- 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include <array>		// C++ 11
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	// int[1000] 이런 자료구조는 앞으론 쓰지 말 것		new int[1000] 이거 쓸 때 말고는 안쓸거임 (이건 기계어 수준이라 안쓸 수 없음)
	// 대체할 수 있는 더 좋은 것이 있기 때문 - 시간/공간의 zero-overhead
	// 컴퓨터 세상에서 더 좋은 것 - 메모리를 덜 잡아먹음. 시간이 덜 걸림.
	// array<T, N>

	// int a[1000];		// 자료형은 int[N] 인데, 이름위치가 비정상

	std::array<int, 1000> a;

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 작은 값과 큰 값을 찾아서 화면에 출력하라.
	// syntatic sugar 인 structured binding을 사용하면 pair를 이렇게 쓸 수 있다!
	auto[작은값, 큰값] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값: " << *작은값 << ", 가장 큰 값: " << *큰값 << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 15:10:34.2820448
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//------------------------------------------------------------------------------------------------------- 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include <array>		// C++ 11
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	// int[1000] 이런 자료구조는 앞으론 쓰지 말 것		new int[1000] 이거 쓸 때 말고는 안쓸거임 (이건 기계어 수준이라 안쓸 수 없음)
	// 대체할 수 있는 더 좋은 것이 있기 때문 - 시간/공간의 zero-overhead
	// 컴퓨터 세상에서 더 좋은 것 - 메모리를 덜 잡아먹음. 시간이 덜 걸림.
	// array<T, N>

	// int a[1000];		// 자료형은 int[N] 인데, 이름위치가 비정상

	std::array<int, 1000> a;

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	auto[minPos, maxPos] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값: " << *minPos << ", 가장 큰 값: " << *maxPos << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 15:11:49.2972866
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//------------------------------------------------------------------------------------------------------- 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include <array>		// C++ 11
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

// ----------
int main()
// ----------
{
	std::array<int, 1000> a;
	std::cout << "a가 사용하는 메모리 - " << sizeof a << std::endl;

	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	auto[minPos, maxPos] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값: " << *minPos << ", 가장 큰 값: " << *maxPos << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-16 15:19:51.6916000
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 10일																					(2주 1일)
//------------------------------------------------------------------------------------------------------- 
// 다음 주 - 많은 수의 자료를 다루기
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++ 23
#include <algorithm>
#include <array>		// C++ 11
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };
// 다음 시간 멤버 설명부터 다시

template <class T, int N>
class myArray {
public:
	size_t size() const {
		return N;
	}

	int& operator[](int idx) {
		return a[idx];
	}

	T* begin() {
		return &a[0];
	}

	T* end() {
		return &a[N];
	}

private:
	T a[N];
};

// ----------
int main()
// ----------
{
	myArray<int, 1000> a;

	for (int i = 0; i < a.size(); ++i) {
		a[i] = uid(dre);
	}

	//for (int& num : a) {
	//	num = uid(dre);
	//}

	for (int num : a) {
		std::print("{:8}", num);
	}
	std::cout << std::endl;

	auto[minPos, maxPos] = std::minmax_element(a.begin(), a.end());
	std::cout << "가장 작은 값: " << *minPos << ", 가장 큰 값: " << *maxPos << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-17 16:01:16.4197878
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 17일																					(3주 1일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <random>
#include "save.h"

std:: default_random_engine dre;
std::uniform_int_distribution<int> uid{ std::numeric_limits<int>::min(), std::numeric_limits<int>::max() };

// ----------
int main()
// ----------
{
	// [문제] 랜덤 int값 천만개를 다음과 같이 "int천만개.txt" 파일에 저장하였다.

	std::ofstream out{ "int천만개.txt" };
	for (int i = 0; i < 100; ++i) {
		out << uid(dre) << ' ';
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-17 16:02:44.8381459
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 17일																					(3주 1일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <random>
#include "save.h"

std:: default_random_engine dre;
std::uniform_int_distribution<int> uid{ std::numeric_limits<int>::min(), std::numeric_limits<int>::max() };

// ----------
int main()
// ----------
{
	// [문제] 랜덤 int값 천만개를 다음과 같이 "int천만개.txt" 파일에 저장하였다.

	std::ofstream out{ "int천만개.txt" };		// 1105 bytes
	for (int i = 0; i < 100; ++i) {
		out << uid(dre) << " ";
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-17 16:05:21.2910890
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 17일																					(3주 1일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <random>
#include "save.h"

std:: default_random_engine dre;
std::uniform_int_distribution<int> uid{ std::numeric_limits<int>::min(), std::numeric_limits<int>::max() };

// ----------
int main()
// ----------
{
	// [문제] 랜덤 int값 천만개를 다음과 같이 "int천만개.txt" 파일에 저장하였다.

	std::ofstream out{ "int천만개.txt" };
	for (int i = 0; i < 10'000'000; ++i) {
		out << uid(dre) << std::endl;
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-17 16:07:10.1597230
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 17일																					(3주 1일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <random>
#include "save.h"

std:: default_random_engine dre;
std::uniform_int_distribution<int> uid{ std::numeric_limits<int>::min(), std::numeric_limits<int>::max() };

// ----------
int main()
// ----------
{
	// [문제] 랜덤 int값 천만개를 다음과 같이 "int천만개.txt" 파일에 저장하였다.

	std::ofstream out{ "int천만개.txt" };			// 119'828'581 bytes
	for (int i = 0; i < 100; ++i) {
		out << uid(dre) << '\n';
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-17 16:07:40.4562318
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 17일																					(3주 1일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <random>
#include "save.h"

std:: default_random_engine dre;
std::uniform_int_distribution<int> uid{ std::numeric_limits<int>::min(), std::numeric_limits<int>::max() };

// ----------
int main()
// ----------
{
	// [문제] 랜덤 int값 천만개를 다음과 같이 "int천만개.txt" 파일에 저장하였다.

	std::ofstream out{ "int천만개.txt" };			// 119'828'581 bytes
	for (int i = 0; i < 10'000'000; ++i) {
		out << uid(dre) << '\n';
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-17 16:20:17.2983777
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 17일																					(3주 1일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include "save.h"

// ----------
int main()
// ----------
{
	// [문제] "int천만개.txt" 파일에는 int값 천만개가 저장되어있다.
	// 천만개가 있다는 사실은 틀림없다 - 믿고 쓰자
	// 가장 작은 값을 찾아 출력하라.

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-17 16:41:02.5228628
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 17일																					(3주 1일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <array>
#include <algorithm>
#include "save.h"

std::array<int, 10'000'000> a;

// ----------
int main()
// ----------
{
	// [문제] "int천만개.txt" 파일에는 int값 천만개가 저장되어있다.
	// 천만개가 있다는 사실은 틀림없다 - 믿고 쓰자
	// 가장 작은 값을 찾아 출력하라.
	std::ifstream in{ "int천만개.txt" };
	if (not in) {
		std::cout << "파일 확인해 보세요" << std::endl;
		return 2020317;
	}

	for (int& num : a) {
		in >> num;
	}

	std::cout << "최솟값: " << *min_element (a.begin(), a.end()) << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-17 16:52:31.7137566
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 17일																					(3주 1일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <algorithm>
#include "save.h"

// ----------
int main()
// ----------
{
	// [문제] "int천만개.txt" 파일에는 int값 천만개가 저장되어있다.
	// 천만개가 있다는 사실은 틀림없다 - 믿고 쓰자
	// 가장 작은 값을 찾아 출력하라.
	std::ifstream in{ "int천만개.txt" };
	if (not in) {
		std::cout << "파일 확인해 보세요" << std::endl;
		return 2020317;
	}

	std::cout << "최솟값: " << *min_element(std::istream_iterator<int>{in}, {}) << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-23 13:34:03.5570654
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 23일																					(3주 2일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// ----------
int main()
// ----------
{
	// [문제] int로 표현할 수 있는 값은 -2,147,483,648 ~ +2,147,483,647 까지이다.
	// 모든 int 값을 하나도 빠짐없이 한 개씩 파일에 text로 기록하였다.
	// 값과 값은 빈칸 한 개로 구분하였다.
	// 이렇게 하면 int 를 한 개 기록하는데 평균 몇 바이트가 필요한지 계산하라.
	int num{ 0x01020304 };
	std::cout << num << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-23 13:43:00.8333015
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 23일																					(3주 2일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid;
// ----------
int main()
// ----------
{
	// [문제] 랜덤 int값 1000개를 화면에 출력하라.
	for (int i{}; i < 1000; ++i) {
		std::cout << uid(dre) << std::endl;
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-23 13:45:14.1764712
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 23일																					(3주 2일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <fstream>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid;
// ----------
int main()
// ----------
{
	// [문제] 랜덤 int값 1000'0000개를 파일 "int천만개"에 저장하라.
	std::ofstream out{ "int천만개" };

	for (int i{}; i < 1000'0000; ++i) {
		out << uid(dre) << '\n';
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-23 13:51:11.4071499
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 23일																					(3주 2일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <fstream>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid;
// ----------
int main()
// ----------
{
	// [문제] 랜덤 int값 1000'0000개를 파일 "int천만개"에 저장하라.
	std::ofstream out{ "int천만개" };		
	// 114'826'203 bytes - text mode
	// 4000'0000 - 메모리 그대로 저장한다면 40MB로 충분함.

	int num;
	for (int i{}; i < 1000'0000; ++i) {
		// out << uid(dre) << '\n';
		num = uid(dre);
		out.write(reinterpret_cast<char*>(&num) , sizeof(int));
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-23 13:54:10.4265857
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 23일																					(3주 2일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <fstream>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid;
// ----------
int main()
// ----------
{
	// [문제] 랜덤 int값 1000'0000개를 파일 "int천만개"에 저장하라.
	std::ofstream out{ "int천만개" , std::ios::binary};		
	// 114'826'203 bytes - text mode
	// 4000'0000 - 메모리 그대로 저장한다면 40MB로 충분함.

	int num;
	for (int i{}; i < 1000'0000; ++i) {
		// out << uid(dre) << '\n';
		num = uid(dre);
		out.write(reinterpret_cast<char*>(&num) , sizeof(int));
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-23 13:58:32.4722268
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 23일																					(3주 2일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <filesystem>			// C++ 17
#include "save.h"

// [문제] 파일 "int천만개"에는 int값 천만개가 저장되어 있다.
// 파일은 binary 모드로 열었고 int 값은 stream의 write 함수를 사용하여 저장하였다.

// ----------
int main()
// ----------
{
	std::cout << std::filesystem::file_size("int천만개") / sizeof(int) << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-23 14:20:30.6835090
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 23일																					(3주 2일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <filesystem>			// C++ 17
#include <array>
#include <algorithm>
#include "save.h"

// [문제] 파일 "int천만개"에는 int값 천만개가 저장되어 있다.
// 파일은 binary 모드로 열었고 int 값은 stream의 write 함수를 사용하여 저장하였다.
// 저장된 int값을 모두 메모리에 저장하라.
// 저장된 값 중에서 가장 작은 값과 큰 값을 화면에 출력하라.

std::array<int, 1000'0000> a;

// ----------
int main()
// ----------
{
	std::ifstream in{ "int천만개", std::ios::binary };
	if (not in) {
		std::cout << "파일에 문제가 있다" << std::endl;
		return 20260323;
	}

	in.read( (char*) (a.data()), a.size() * sizeof(int) );

	auto [minPos, maxPos] = minmax_element(a.begin(), a.end());
	std::cout << "작은 값 - " << *minPos << ", 큰 값 - " << *maxPos << std::endl;
	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-23 14:47:05.4040237
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 23일																					(3주 2일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O - class 객체
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"
#include <random>
#include <string>
#include <print>

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };
std::uniform_int_distribution uidNameLen{ 10, 30 };
std::uniform_int_distribution<int> uidChar{ 'a', 'z' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}

private:
	std::string name;		// [10, 30] 사이의 소문자로 구성된 이름
	size_t id;						// [0, 9999]


	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:>4}] - {}", dog.id, dog.name);
		return os;
	}
};

// ----------
int main()
// ----------
{
	Dog dog;

	std::cout << dog << std::endl;
	
	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-23 14:48:00.3082426
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 23일																					(3주 2일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O - class 객체
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"
#include <random>
#include <string>
#include <print>
#include <array>

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };
std::uniform_int_distribution uidNameLen{ 10, 30 };
std::uniform_int_distribution<int> uidChar{ 'a', 'z' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}

private:
	std::string name;		// [10, 30] 사이의 소문자로 구성된 이름
	size_t id;						// [0, 9999]


	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:>4}] - {}", dog.id, dog.name);
		return os;
	}
};

// ----------
int main()
// ----------
{
	std::array<Dog, 1000> dogs;

	for (Dog dog : dogs) {
		std::cout << dog << std::endl;
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-23 14:54:30.4720546
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 23일																					(3주 2일)
//------------------------------------------------------------------------------------------------------- 
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O - class 객체
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"
#include <random>
#include <string>
#include <print>
#include <array>
#include <fstream>

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };
std::uniform_int_distribution uidNameLen{ 10, 30 };
std::uniform_int_distribution<int> uidChar{ 'a', 'z' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}

private:
	std::string name;		// [10, 30] 사이의 소문자로 구성된 이름
	size_t id;						// [0, 9999]


	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:>4}] - {}", dog.id, dog.name);
		return os;
	}
};

// ----------
int main()
// ----------
{
	std::array<Dog, 1000> dogs;

	std::ofstream out{ "Dog천마리", std::ios::binary };
	out.write((char*)dogs.data(), dogs.size() * sizeof(Dog));

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-24 16:22:22.1539205
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 24일																					(4주 1일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// [문제] 사용자가 입력한 수만큼 int를 저장할 메모리를 확보하라.
// 1부터 시작하는 정수로 메모리를 채워라.
// 합계를 화면에 출력하라.

// [메모]
// vector 사용했을 때 메모리 극한으로 사용하는 법
/*
- 메모리 낭비를 줄이는 STL의 전략
메모리를 딱 맞춰서 쓰고 싶다면 다음 두 가지 방법을 사용할 수 있습니다.

① reserve()로 미리 딱 맞게 예약하기
사용자가 입력한 count를 미리 알고 있다면, reserve(count)를 호출하여 딱 그만큼만 메모리를 할당하도록 유도할 수 있습니다.

② shrink_to_fit()으로 남는 공간 반납하기 (C++11 이상)
데이터를 다 채운 뒤, v.shrink_to_fit()을 호출하면 capacity를 현재 size에 맞춰 줄여줍니다. 즉, new로 할당한 것과 똑같은 메모리 상태를 만듭니다.
*/

// ----------
int main()
// ----------
{
    int count;
    std::cout << "숫자 몇까지 저장할 건지 입력하세요 : ";
    std::cin >> count;
    if (count < 0) {
        std::cout << "음수는 안돼요" << std::endl;
        return 20260324;
    }
    int* arr = new int[count];

    for (int i = 0; i < count; ++i) {
        arr[i] = i + 1;
    }

    int sum = 0;
    for (int i = 0; i < count; ++i) {
        sum += arr[i];
    }
    std::cout << sum;

    delete[]arr;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-24 16:39:59.7057883
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 24일																					(4주 1일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// [문제] 사용자가 입력한 수만큼 int를 저장할 메모리를 확보하라.
// 1부터 시작하는 정수로 메모리를 채워라.
// 합계를 화면에 출력하라.

// [메모]
// vector 사용했을 때 메모리 극한으로 사용하는 법
/*
- 메모리 낭비를 줄이는 STL의 전략
메모리를 딱 맞춰서 쓰고 싶다면 다음 두 가지 방법을 사용할 수 있습니다.

① reserve()로 미리 딱 맞게 예약하기
사용자가 입력한 count를 미리 알고 있다면, reserve(count)를 호출하여 딱 그만큼만 메모리를 할당하도록 유도할 수 있습니다.

② shrink_to_fit()으로 남는 공간 반납하기 (C++11 이상)
데이터를 다 채운 뒤, v.shrink_to_fit()을 호출하면 capacity를 현재 size에 맞춰 줄여줍니다. 즉, new로 할당한 것과 똑같은 메모리 상태를 만듭니다.
*/

// ----------
int main()
// ----------
{
	std::cout << "int 몇 개가 필요하신지? ";
	size_t num;
	std::cin >> num;

	// 설마 음수를 입력하지는 않으리라는 강력한 희망을!! (검사 안하겠다는 뜻)

	int* p;									// 스택에 들어가는 객체
	try {									// 만약 더 이상 할당할 수 있는 메모리가 없으면 뻗어버림. (기절) 그래서 일단 try 해보고 할당 안되면 catch로 감.
		p = new int[num];			// 시작 주소를 리턴하고 그 주소로부터 int * num크기만큼 메모리를 할당해줌!
	}
	catch (std::exception& e) {
		std::cout << "메모리 고갈 - " << e.what() << std::endl;
	}

	for (int i = 0; i < num; ++i) {
		p[i] = i + 1;
	}

	int sum{};
	for (int i = 0; i < num; ++i) {
		sum += p[i];
	}

	std::cout << "1부터 " << num << "까지의 합계 : " << sum << std::endl;
	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-30 13:40:38.5851899
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 24일																					(4주 1일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
// smart pointer -> callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"
#include <numeric>
#include <memory>

// [문제] 사용자가 입력한 수만큼 int를 저장할 메모리를 확보하라.
// 1부터 시작하는 정수로 메모리를 채워라.
// 합계를 화면에 출력하라.

// [메모]
// vector 사용했을 때 메모리 극한으로 사용하는 법
/*
- 메모리 낭비를 줄이는 STL의 전략
메모리를 딱 맞춰서 쓰고 싶다면 다음 두 가지 방법을 사용할 수 있습니다.

① reserve()로 미리 딱 맞게 예약하기
사용자가 입력한 count를 미리 알고 있다면, reserve(count)를 호출하여 딱 그만큼만 메모리를 할당하도록 유도할 수 있습니다.

② shrink_to_fit()으로 남는 공간 반납하기 (C++11 이상)
데이터를 다 채운 뒤, v.shrink_to_fit()을 호출하면 capacity를 현재 size에 맞춰 줄여줍니다. 즉, new로 할당한 것과 똑같은 메모리 상태를 만듭니다.
*/

// ----------
int main()
// ----------
{
	// c++ 옵티마이저 -> int* p를 자동으로 while 루프 밖으로 빼줌. (만들고 지우고 만들고 지우고 하면 알아서 밖으로 빼줌)
	// int* p;	스택에 들어가는 객체 -> 사용하지 말아야 함
	// C++11(Modern C++)에 이것의 완벽한 대체 수단이 있기 때문에 -> smart pointer

	std::unique_ptr<int[]> p;

	while (true) {
		std::cout << "int 몇 개가 필요하신지? ";
		size_t num;
		std::cin >> num;

		// 설마 음수를 입력하지는 않으리라는 강력한 희망을!! (검사 안하겠다는 뜻)

		try {									// 만약 더 이상 할당할 수 있는 메모리가 없으면 뻗어버림. (기절) 그래서 일단 try 해보고 할당 안되면 catch로 감.
			p.reset(new int[num]);			// 시작 주소를 리턴하고 그 주소로부터 int * num크기만큼 메모리를 할당해줌!
		}
		catch (std::exception& e) {
			std::cout << "메모리 고갈 - " << e.what() << std::endl;
		}

		//for (int i = 0; i < num; ++i) {
		//	p[i] = i + 1;
		//}
		std::iota(p.get(), p.get() + num, 1);			// p부터 p + num까지 1씩 증가하며 채우기

		//for (int i = 0; i < num; ++i) {
		//	sum += p[i];
		//}
		long long sum = std::accumulate(p.get(), p.get() + num, 0LL);		// p부터 p + num까지 더하기 (마지막 인자는 항등원)

		std::cout << "1부터 " << num << "까지의 합계 : " << sum << std::endl;
		save("메인.cpp");
	}
	
	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-30 13:42:23.6081257
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
// smart pointer -> callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// ----------
int main()
// ----------
{
	int* p;

	p = new int;

	std::cout << *p << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-30 13:42:41.2690052
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
// smart pointer -> callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// ----------
int main()
// ----------
{
	int* p;

	p = new int{20260330};

	std::cout << *p << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-30 13:44:12.0988658
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
// smart pointer -> callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// ----------
int main()
// ----------
{
	int* p;

	p = new int{20260330};

	std::cout << *p << std::endl;

	save("메인.cpp");

	delete p;
	delete p;
}

==========================================================================================
저장한 시간 - 2026-03-30 13:54:54.5865516
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
// smart pointer -> callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// C++ 언어의 보증사항
// 프로그램이 정상 종료시 (return 문, exit과는 다름) 스택에 생성된 지역객체를
// 반드시 소멸됨을 보증한다.

class Dog {
public:
	Dog() { std::cout << "생성" << std::endl; }
	~Dog() { std::cout << "소멸" << std::endl; }
};

// ----------
int main()
// ----------
{
	save("메인.cpp");

	Dog* p = new Dog;

	delete p;
}

==========================================================================================
저장한 시간 - 2026-03-30 13:55:34.9589920
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
// smart pointer -> callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// C++ 언어의 보증사항
// 프로그램이 정상 종료시 (return 문, exit과는 다름) 스택에 생성된 지역객체를
// 반드시 소멸됨을 보증한다.

class Dog {
public:
	Dog() { std::cout << "생성" << std::endl; }
	~Dog() { std::cout << "소멸" << std::endl; }
};

// ----------
int main()
// ----------
{
	save("메인.cpp");

	Dog* p = new Dog;

	return 0;	// 과연 소멸됨을 보증할까? 

	delete p;
}

==========================================================================================
저장한 시간 - 2026-03-30 13:59:48.6789303
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
// smart pointer -> callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <memory>
#include "save.h"

// C++ 언어의 보증사항
// 프로그램이 정상 종료시 (return 문, exit과는 다름) 스택에 생성된 지역객체를
// 반드시 소멸됨을 보증한다.

class Dog {
public:
	Dog() { std::cout << "생성" << std::endl; }
	~Dog() { std::cout << "소멸" << std::endl; }
};

// ----------
int main()
// ----------
{
	save("메인.cpp");

	// T* 는 raw-pointer이다. 앞으로 쓰지 말자

	// Dog* p = new Dog;
	std::unique_ptr<Dog> p{ new Dog };		// RAII

	return 0;	// 과연 소멸됨을 보증할까? -> 아닌데??

	std::cout << "이 문장이 출력되면 노트북을 버려라" << std::endl;
}

==========================================================================================
저장한 시간 - 2026-03-30 14:04:25.7176596
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
// smart pointer -> callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <memory>
#include "save.h"

// C++ 언어의 보증사항
// 프로그램이 정상 종료시 (return 문, exit과는 다름) 스택에 생성된 지역객체를
// 반드시 소멸됨을 보증한다.

class Dog {
public:
	Dog() { std::cout << "생성" << std::endl; }
	~Dog() { std::cout << "소멸" << std::endl; }
};

class 스마트포인터 {
private:
	Dog* p;
public:
	스마트포인터(Dog* p) : p{ p } {}
	~스마트포인터() {
		delete p;
	}
};

// ----------
int main()
// ----------
{
	save("메인.cpp");

	// T* 는 raw-pointer이다. 앞으로 쓰지 말자

	// Dog* p = new Dog;
	스마트포인터 p{ new Dog };		// RAII

	return 0;	// 과연 소멸됨을 보증할까? -> 아닌데??

	std::cout << "이 문장이 출력되면 노트북을 버려라" << std::endl;
}

==========================================================================================
저장한 시간 - 2026-03-30 14:11:09.2347151
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// Dynamic memory allocation (동적 메모리 할당)
// smart pointer -> callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <memory>
#include "save.h"

// C++ 언어의 보증사항
// 프로그램이 정상 종료시 (return 문, exit과는 다름) 스택에 생성된 지역객체를
// 반드시 소멸됨을 보증한다.
// stack-unwiding

class Dog {
public:
	Dog() { std::cout << "생성" << std::endl; }
	~Dog() { std::cout << "소멸" << std::endl; }
};


// ----------
int main()
// ----------
{
	std::unique_ptr<Dog[]> p {new Dog[10]};

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-30 14:30:46.4174093
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// [문제] "메인.cpp"의 소문자를 모두 대문자로 바꾼 "메인대문자.cpp"를 만들어라.

// ----------
int main()
// ----------
{


	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-30 14:49:19.8643717
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <algorithm>
#include "save.h"

// [문제] "메인.cpp"의 소문자를 모두 대문자로 바꾼 "메인대문자.cpp"를 만들어라.
char 소투대(char c) {
	return c = toupper(c);
}

// ----------
int main()
// ----------
{
	std::ifstream in{ "메인.cpp" };
	if (not in) {
		return 20260330;
	}

	std::ofstream out{ "메인대문자.cpp" };

	transform(std::istreambuf_iterator<char>{in}, {}, std::ostreambuf_iterator<char>{out}, 소투대);

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-30 14:51:21.7078449
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <algorithm>
#include "save.h"

// [문제] "메인.cpp"의 소문자를 모두 대문자로 바꾼 "메인대문자.cpp"를 만들어라.
char 소투대(char c) {
	return c = toupper(c);
}

// ----------
int main()
// ----------
{
	std::ifstream in{ "메인.cpp" };
	if (not in) {
		return 20260330;
	}

	std::ofstream out{ "메인대문자.cpp" };

	transform(std::istreambuf_iterator<char>{in}, {}, std::ostreambuf_iterator<char>{out}, 
		[](char c) {
		return c = toupper(c);
		});

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-30 15:15:23.0430209
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <array>
#include "save.h"

// [문제] 랜덤값을 갖는 int 1000만개를 메모리에 저장하라.
// qsort를 이용하여 오름차순 정렬하라.
// 정렬결과를 앞에서부터 1000개만 화면에 출력하라.

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };

std::array<int, 1000'0000> a;

int 오름차순(const void* a, const void* b) {
    return (*(int*)a - *(int*)b);
}

// ----------
int main()
// ----------
{
    save("메인.cpp");

    for (int& num : a) {
        num = uid(dre);
   }

    // 시간측정시작
    qsort(a.data(), a.size(), sizeof(std::array<int, 1000'0000>::value_type), 오름차순);
    // 측정 끝
	
    std::cout << "정렬 후 출력" << std::endl;
    for (int num : a) {
        std::print("{:8}", num);
    }
    std::cout << std::endl;
}

==========================================================================================
저장한 시간 - 2026-03-30 15:16:29.3946423
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <array>
#include "save.h"

// [문제] 랜덤값을 갖는 int 1000만개를 메모리에 저장하라.
// qsort를 이용하여 오름차순 정렬하라.
// 정렬결과를 앞에서부터 1000개만 화면에 출력하라.

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };

std::array<int, 1000'0000> a;

int 오름차순(const void* a, const void* b) {
    return (*(int*)a - *(int*)b);
}

// ----------
int main()
// ----------
{
    save("메인.cpp");

    for (int& num : a) {
        num = uid(dre);
   }

    // 시간측정시작
    qsort(a.data(), a.size(), sizeof(std::array<int, 1000'0000>::value_type), 오름차순);
    // 측정 끝
	
    std::cout << "정렬 후 출력" << std::endl;
    //for (int num : a) {
    //    std::print("{:8}", num);
    //}
    for (int i = 0; i < 1000; ++i) {
        std::print("{:8}", a[i]);
    }
    std::cout << std::endl;
}

==========================================================================================
저장한 시간 - 2026-03-30 15:19:39.1887091
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <array>
#include <ranges>
#include "save.h"

// [문제] 랜덤값을 갖는 int 1000만개를 메모리에 저장하라.
// qsort를 이용하여 오름차순 정렬하라.
// 정렬결과를 앞에서부터 1000개만 화면에 출력하라.

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };

std::array<int, 1000'0000> a;

int 오름차순(const void* a, const void* b) {
    return (*(int*)a - *(int*)b);
}

// ----------
int main()
// ----------
{
    save("메인.cpp");

    for (int& num : a) {
        num = uid(dre);
   }

    // 시간측정시작
    qsort(a.data(), a.size(), sizeof(std::array<int, 1000'0000>::value_type), 오름차순);
    // 측정 끝
	
    std::cout << "정렬 후 출력" << std::endl;
    for (int num : a | std::views::take(1000)) {      // | : 버티컬 바 (짝대기) -> 필터 역할 (데이터를 원하는 대로 뽑아줄 수 있음!!)
        std::print("{:8}", num);
    }
    //for (int i = 0; i < 1000; ++i) {
    //    std::print("{:8}", a[i]);
    //}
    std::cout << std::endl;
}

==========================================================================================
저장한 시간 - 2026-03-30 15:20:33.2716931
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 30일																					(4주 2일)
//------------------------------------------------------------------------------------------------------- 
// callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <array>
#include <ranges>
#include "save.h"

// [문제] 랜덤값을 갖는 int 1000만개를 메모리에 저장하라.
// qsort를 이용하여 오름차순 정렬하라.
// 정렬결과를 앞에서부터 1000개만 화면에 출력하라.

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };

std::array<int, 1000'0000> a;

int 오름차순(const void* a, const void* b) {
    return (*(int*)a - *(int*)b);
}

// ----------
int main()
// ----------
{
    save("메인.cpp");

    for (int& num : a) {
        num = uid(dre);
   }

    // 시간측정시작
    qsort(a.data(), a.size(), sizeof(std::array<int, 1000'0000>::value_type), 오름차순);
    // 측정 끝
	
    std::cout << "정렬 후 출력" << std::endl;

    for (int num : a | std::views::reverse
                                | std::views::take(1000)) {      // | : 버티컬 바 (짝대기) -> 필터 역할 (데이터를 원하는 대로 뽑아줄 수 있음!!)
        std::print("{:8}", num);
    }

    std::cout << std::endl;
}

==========================================================================================
저장한 시간 - 2026-03-31 15:38:43.0248566
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 31일																					(5주 1일)
//------------------------------------------------------------------------------------------------------- 
// callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <array>
#include <ranges>
#include <chrono>
#include "save.h"

// [문제] 랜덤값을 갖는 int 1000만개를 메모리에 저장하라.
// qsort를 이용하여 오름차순 정렬하라.
// 정렬결과를 앞에서부터 1000개만 화면에 출력하라.

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };

std::array<int, 1000'0000> a;

int 오름차순(const void* a, const void* b) {
    return (*(int*)a - *(int*)b);
}

// ----------
int main()
// ----------
{
    save("메인.cpp");

    for (int& num : a) {
        num = uid(dre);
   }

    // 시간측정시작
    auto start = std::chrono::high_resolution_clock::now();
    qsort(a.data(), a.size(), sizeof(int), 오름차순);
    auto stop = std::chrono::high_resolution_clock::now();

    std::cout << "걸린 시간 - " << stop - start << std::endl;
    // 측정 끝
	
    std::cout << "정렬 후 출력" << std::endl;

    for (int num : a | std::views::reverse
                                | std::views::take(1000)) {      // | : 버티컬 바 (짝대기) -> 필터 역할 (데이터를 원하는 대로 뽑아줄 수 있음!!)
        std::print("{:8}", num);
    }

    std::cout << std::endl;
}

==========================================================================================
저장한 시간 - 2026-03-31 15:41:28.1778373
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 31일																					(5주 1일)
//------------------------------------------------------------------------------------------------------- 
// callable type -> 실습 -> STL
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <array>
#include <ranges>
#include <chrono>
#include "save.h"

// [문제] 랜덤값을 갖는 int 1000만개를 메모리에 저장하라.
// qsort를 이용하여 오름차순 정렬하라.
// 정렬결과를 앞에서부터 1000개만 화면에 출력하라.

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };

std::array<int, 1000'0000> a;

int 오름차순(const void* a, const void* b) {
    return (*(int*)a - *(int*)b);
}

// ----------
int main()
// ----------
{
    save("메인.cpp");

    for (int& num : a) {
        num = uid(dre);
   }

    // 시간측정시작
    auto start = std::chrono::high_resolution_clock::now();
    qsort(a.data(), a.size(), sizeof(int), 오름차순);
    auto stop = std::chrono::high_resolution_clock::now();
    // 측정 끝

    auto duration = std::chrono::duration_cast<std::chrono::milliseconds>(stop - start);
    std::cout << "걸린 시간 - " << duration << std::endl;
	
    std::cout << std::endl;
}

==========================================================================================
저장한 시간 - 2026-03-31 16:09:32.7553313
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 31일																					(5주 1일)
//------------------------------------------------------------------------------------------------------- 
// 호출가능 타입 - callable types
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <array>
#include <chrono>
#include <algorithm>
#include "save.h"

// [문제] 랜덤값을 갖는 int 1000만개를 메모리에 저장하라.
// qsort를 이용하여 오름차순 정렬하라.
// 정렬결과를 앞에서부터 1000개만 화면에 출력하라.

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };

std::array<int, 1000'0000> a;

int 오름차순(const void* a, const void* b) {
    return (*(int*)a - *(int*)b);
}

bool sort용_오름차순(int a, int b)
{
    return a < b;
}

// ----------
int main()
// ----------
{
    save("메인.cpp");
    {
        for (int& num : a) {
            num = uid(dre);
        }

        // 시간측정시작
        auto start = std::chrono::high_resolution_clock::now();
        qsort(a.data(), a.size(), sizeof(int), 오름차순);
        auto stop = std::chrono::high_resolution_clock::now();
        // 측정 끝

        auto duration = std::chrono::duration_cast<std::chrono::milliseconds>(stop - start);
        std::cout << "qsort 시간 - " << duration << std::endl;
    }

    {
        for (int& num : a) {
            num = uid(dre);
        }

        // 시간측정시작
        auto start = std::chrono::high_resolution_clock::now();
        std::sort(a.begin(), a.end(), sort용_오름차순);
        auto stop = std::chrono::high_resolution_clock::now();
        // 측정 끝

        auto duration = std::chrono::duration_cast<std::chrono::milliseconds>(stop - start);
        std::cout << "sort 시간 - " << duration << std::endl;
    }

    std::cout << std::endl;
}

==========================================================================================
저장한 시간 - 2026-03-31 16:18:14.0113883
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 31일																					(5주 1일)
//------------------------------------------------------------------------------------------------------- 
// 호출가능 타입 - callable types
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <array>
#include <chrono>
#include <algorithm>
#include "save.h"

// [문제] 랜덤값을 갖는 int 1000만개를 메모리에 저장하라.
// qsort를 이용하여 오름차순 정렬하라.
// 정렬결과를 앞에서부터 1000개만 화면에 출력하라.

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };

std::array<int, 1000'0000> a;

int 오름차순(const void* a, const void* b) {
    return (*(int*)a - *(int*)b);
}

bool sort용_오름차순(int a, int b)
{
    return a < b;
}

// ----------
int main()
// ----------
{
    save("메인.cpp");
    {
        for (int& num : a) {
            num = uid(dre);
        }

        // 시간측정시작
        auto start = std::chrono::high_resolution_clock::now();
        qsort(a.data(), a.size(), sizeof(int), 오름차순);
        auto stop = std::chrono::high_resolution_clock::now();
        // 측정 끝

        auto duration = std::chrono::duration_cast<std::chrono::milliseconds>(stop - start);
        std::cout << "qsort 시간 - " << duration << std::endl;
    }

    {
        for (int& num : a) {
            num = uid(dre);
        }

        // 시간측정시작
        auto start = std::chrono::high_resolution_clock::now();
        std::sort(a.begin(), a.end(), [](int a, int b) {
            return a < b;
            });
        auto stop = std::chrono::high_resolution_clock::now();
        // 측정 끝

        auto duration = std::chrono::duration_cast<std::chrono::milliseconds>(stop - start);
        std::cout << "sort 시간 - " << duration << std::endl;
    }

    std::cout << std::endl;
}

==========================================================================================
저장한 시간 - 2026-03-31 16:44:00.1348655
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 31일																					(5주 1일)
//------------------------------------------------------------------------------------------------------- 
// 호출가능 타입 - callable types
// 1. 함수
// 2. 람다
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

void f() {
    std::cout << "난 호출 가능해요" << std::endl;
}

// ----------
int main()
// ----------
{
    f();

    save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-31 16:46:28.7139027
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 31일																					(5주 1일)
//------------------------------------------------------------------------------------------------------- 
// 호출가능 타입 - callable types
// 1. 함수
// 2. 람다
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

void f() {
    std::cout << "난 호출 가능해요" << std::endl;
}

// ----------
int main()
// ----------
{
    int n;      // addressof, sizeof, typeid

    std::cout << std::addressof(f) << std::endl;
    std::cout << typeid(f).name() << std::endl;

    save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-31 16:52:14.1771741
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 31일																					(5주 1일)
//------------------------------------------------------------------------------------------------------- 
// 호출가능 타입 - callable types
// 1. 함수
// 2. 람다
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

void f() {
    std::cout << "난 호출 가능해요" << std::endl;
}

// ----------
int main()
// ----------
{
    void (*a)() = f;        // a는 함수 f를 가리키는 포인터

    a();

    save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-31 16:58:31.1497429
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 31일																					(5주 1일)
//------------------------------------------------------------------------------------------------------- 
// 호출가능 타입 - callable types
// 1. 함수
//      - 함수포인터도 호출가능하다
// 2. 람다
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

void 점프()
{
    std::cout << "점프" << std::endl;
}

void 슬라이드()
{
    std::cout << "슬라이드" << std::endl;
}

// ----------
int main()
// ----------
{
    void (*왼쪽키)() = 점프;

    왼쪽키();
    왼쪽키();
    왼쪽키();

    save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-31 16:59:04.0172228
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 31일																					(5주 1일)
//------------------------------------------------------------------------------------------------------- 
// 호출가능 타입 - callable types
// 1. 함수
//      - 함수포인터도 호출가능하다
// 2. 람다
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

void 점프()
{
    std::cout << "점프" << std::endl;
}

void 슬라이드()
{
    std::cout << "슬라이드" << std::endl;
}

// ----------
int main()
// ----------
{
    void (*왼쪽키)() = 점프;

    왼쪽키();
    왼쪽키();
    // 설정에서 키 기능 변경
    왼쪽키 = 슬라이드;
    왼쪽키();

    save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-31 17:00:20.3169383
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 31일																					(5주 1일)
//------------------------------------------------------------------------------------------------------- 
// 호출가능 타입 - callable types
// 1. 함수
//      - 함수포인터도 호출가능하다
// 2. 람다
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

void 점프()
{
    std::cout << "점프" << std::endl;
}

void 슬라이드()
{
    std::cout << "슬라이드" << std::endl;
}

// ----------
int main()
// ----------
{
    (*점프)();

    save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-31 17:03:55.5666521
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 31일																					(5주 1일)
//------------------------------------------------------------------------------------------------------- 
// 호출가능 타입 - callable types
// 1. 함수
//      - 함수포인터도 호출가능하다
// 2. 람다
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

void 점프()
{
    std::cout << "점프" << std::endl;
}

void 슬라이드()
{
    std::cout << "슬라이드" << std::endl;
}

// ----------
int main()
// ----------
{
    (*점프)();        // 이게 진짜 함수호출
    // 점프()         // 이건 야매로 하는 호출!

    save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-31 17:04:08.9708407
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 31일																					(5주 1일)
//------------------------------------------------------------------------------------------------------- 
// 호출가능 타입 - callable types
// 1. 함수
//      - 함수포인터도 호출가능하다
// 2. 람다
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

void 점프()
{
    std::cout << "점프" << std::endl;
}

void 슬라이드()
{
    std::cout << "슬라이드" << std::endl;
}

// ----------
int main()
// ----------
{
    []() {};

    save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-31 17:04:41.0974292
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 31일																					(5주 1일)
//------------------------------------------------------------------------------------------------------- 
// 호출가능 타입 - callable types
// 1. 함수
//      - 함수포인터도 호출가능하다
// 2. 람다
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

void 점프()
{
    std::cout << "점프" << std::endl;
}

void 슬라이드()
{
    std::cout << "슬라이드" << std::endl;
}

// ----------
int main()
// ----------
{
    []() {
        std::cout << "안녕 난 람다야 반가워" << std::endl;
    };

    save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-31 17:05:52.5029863
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 31일																					(5주 1일)
//------------------------------------------------------------------------------------------------------- 
// 호출가능 타입 - callable types
// 1. 함수
//      - 함수포인터도 호출가능하다
// 2. 람다
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

void 점프()
{
    std::cout << "점프" << std::endl;
}

void 슬라이드()
{
    std::cout << "슬라이드" << std::endl;
}

// ----------
int main()
// ----------
{
    []() {
        std::cout << "안녕 난 람다야 반가워" << std::endl;
    }();

    save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-31 17:08:32.7508287
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 31일																					(5주 1일)
//------------------------------------------------------------------------------------------------------- 
// 호출가능 타입 - callable types
// 1. 함수
//      - 함수포인터도 호출가능하다
// 2. 람다
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

void 점프()
{
    std::cout << "점프" << std::endl;
}

void 슬라이드()
{
    std::cout << "슬라이드" << std::endl;
}

// ----------
int main()
// ----------
{
    std::cout << typeid([]() {
        std::cout << "안녕 난 람다야 반가워" << std::endl;
        }).name() << std::endl;

    save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-31 17:14:02.3491971
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 31일																					(5주 1일)
//------------------------------------------------------------------------------------------------------- 
// 호출가능 타입 - callable types
// 1. 함수
//      - 함수포인터도 호출가능하다
// 2. 람다
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

void 점프()
{
    std::cout << "점프" << std::endl;
}

void 슬라이드()
{
    std::cout << "슬라이드" << std::endl;
}

// ----------
int main()
// ----------
{
    class Sakura {
    public:
        void operator()() {
            std::cout << "안녕 난 람다야 반가워" << std::endl;
            };
    };

    Sakura a;
    a();

    std::cout << typeid(a).name() << std::endl;

    save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-03-31 17:15:20.6918221
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		3월 31일																					(5주 1일)
//------------------------------------------------------------------------------------------------------- 
// 호출가능 타입 - callable types
// 1. 함수
//      - 함수포인터도 호출가능하다
// 2. 람다
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"
class Sakura {
public:
    void operator()() {
        std::cout << "안녕 난 람다야 반가워" << std::endl;
    };
};

// ----------
int main()
// ----------
{

    Sakura a;
    a();

    std::cout << typeid(a).name() << std::endl;

    save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-06 13:34:36.5659610
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 6일														(5주 2일)
//------------------------------------------------------------------------------------------------------- 
// 호출가능 타입
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <algorithm>
#include "save.h"

// ----------
int main()
// ----------
{
    std::array<int, 10> a{ 8, 4, 2, 0, 1, 9, 7, 5, 6, 3 };

    sort(a.begin(), a.end());       // 3번째 인자에 int 기준 오름차순 이 기본으로 깔림.

    for (int num : a)
        std::cout << num << ' ';
    std::cout << std::endl;

    save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-06 13:36:53.3242833
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 6일														(5주 2일)
//------------------------------------------------------------------------------------------------------- 
// 호출가능 타입
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <algorithm>
#include "save.h"

bool 정렬기준(int a, int b)
{
    std::cout << "함수 ";     // 10 * log10 (3 ~ 4정도) 번 호출.
    return a < b;
}

// ----------
int main()
// ----------
{
    std::array<int, 10> a{ 8, 4, 2, 0, 1, 9, 7, 5, 6, 3 };

    sort(a.begin(), a.end(), 정렬기준);       // 3번째 인자에 int 기준 오름차순 이 기본으로 깔림.

    for (int num : a)
        std::cout << num << ' ';
    std::cout << std::endl;

    save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-06 13:38:23.3377506
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 6일														(5주 2일)
//------------------------------------------------------------------------------------------------------- 
// 호출가능 타입
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <algorithm>
#include "save.h"

bool 정렬기준(int a, int b)
{
    std::cout << "함수 ";     // 10 * log10 (3 ~ 4정도) 번 호출.
    return a < b;
}

// ----------
int main()
// ----------
{
    std::array<int, 10> a{ 8, 4, 2, 0, 1, 9, 7, 5, 6, 3 };

    // sort(a.begin(), a.end(), 정렬기준);       // 3번째 인자에 int 기준 오름차순 이 기본으로 깔림.
    sort(a.begin(), a.end(), [](int a, int b) {
		std::cout << "람다 ";
        return a < b;
        });
    std::cout << std::endl;
    for (int num : a)
        std::cout << num << ' ';
    std::cout << std::endl;

    save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-06 13:40:50.6005569
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 6일														(5주 2일)
//------------------------------------------------------------------------------------------------------- 
// 호출가능 타입
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <algorithm>
#include "save.h"

bool 정렬기준(int a, int b)
{
    std::cout << "함수 ";     // 10 * log10 (3 ~ 4정도) 번 호출.
    return a < b;
}

class Dog {
public:
    bool operator()(int a, int b) {
        std::cout << "도그 ";
        return a < b;
    }
};

// ----------
int main()
// ----------
{
    std::array<int, 10> a{ 8, 4, 2, 0, 1, 9, 7, 5, 6, 3 };

    // sort(a.begin(), a.end(), 정렬기준);       // 3번째 인자에 int 기준 오름차순 이 기본으로 깔림.
    /*sort(a.begin(), a.end(), [](int a, int b) {
		std::cout << "람다 ";
        return a < b;
        });*/

    sort(a.begin(), a.end(), Dog{} );

    std::cout << std::endl;
    for (int num : a)
        std::cout << num << ' ';
    std::cout << std::endl;

    save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-06 13:49:39.4252671
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 6일														(5주 2일)
//------------------------------------------------------------------------------------------------------- 
// 호출가능 타입 - callable types
// function을 사용하여 모든 호출가능 타입을 표현할 수 있다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <algorithm>
#include "save.h"

bool 정렬기준(int a, int b)
{
    std::cout << "함수 ";     // 10 * log10 (3 ~ 4정도) 번 호출.
    return a < b;
}

class Dog {     // function object
public:
    bool operator()(int a, int b) {
        std::cout << "도그 ";
        return a < b;
    }
};

// ----------
auto main() -> int  // 함수 선언 제 2형식 (가능은 하지만 미친놈임)
// ----------
{
    std::array<int, 10> a{ 8, 4, 2, 0, 1, 9, 7, 5, 6, 3 };

    // sort(a.begin(), a.end(), 정렬기준);       // 3번째 인자에 int 기준 오름차순 이 기본으로 깔림.
	sort(a.begin(), a.end(), [](int a, int b) -> bool {     // [] : lambda introducer -> (반환형) 함수 선언 제 2형식
        std::cout << "람다 ";
        return a < b;       // 컴파일러가 반환형 bool로 알아서 유추
        });

    // sort(a.begin(), a.end(), Dog{} );

    std::cout << std::endl;
    for (int num : a)
        std::cout << num << ' ';
    std::cout << std::endl;

    save("메인.cpp");
}

//sort<RanIt>(a.begin(), a.end(), Callable f) // 3번째 인자가 뭐든, Callable 하기만 하면 됨.
//{
//    f(*a.begin(), *a.begin() + 5);
//
//
//}

==========================================================================================
저장한 시간 - 2026-04-06 14:04:28.4140698
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 6일														(5주 2일)
//------------------------------------------------------------------------------------------------------- 
// 호출가능 타입 - callable types
// function을 사용하여 모든 호출가능 타입을 표현할 수 있다.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <algorithm>
#include <functional>
#include "save.h"

bool 정렬기준(int a, int b)     // 함수는 중괄호를 내려서 쓰는 것이다!
{
    std::cout << "함수 ";     // 10 * log10 (3 ~ 4정도) 번 호출.
    return a < b;
}

class Dog {     // function object
public:
    bool operator()(int a, int b) {
        std::cout << "도그 ";
        return a < b;
    }
};

// ----------
int main()
// ----------
{
	std::array<int, 10> a{ 8, 4, 2, 0, 1, 9, 7, 5, 6, 3 };      // 40bytes

	std::function<bool(int, int)> f;        // 이름, 리턴타입, 인자 (function의 기본) -> 64bytes

    f = 정렬기준;
    f = [](int a, int b) -> bool {
        std::cout << "람다 ";
        return a < b;
        };
    f = Dog{};

	sort(a.begin(), a.end(), f);        // 이러면 도그로 정렬

    std::cout << std::endl;
    for (int num : a)
        std::cout << num << ' ';
    std::cout << std::endl;

    save("메인.cpp");
}

//sort<RanIt>(a.begin(), a.end(), Callable f) // 3번째 인자가 뭐든, Callable 하기만 하면 됨.
//{
//    f(*a.begin(), *a.begin() + 5);
//
//
//}

==========================================================================================
저장한 시간 - 2026-04-06 14:14:21.6573328
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 6일														(5주 2일)
//------------------------------------------------------------------------------------------------------- 
// 예제
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <print>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };	// printable ASCII 코드의 전 범위

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}
private:
	std::string name;	// [1,150]
	int id;				// [0, 999'9999]

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:7}] - {}", dog.id, dog.name);	// 콘솔 말고 os에 출력
		return os;
	}
};

// ----------
int main()
// ----------
{
	for (int i = 0; i < 10; ++i) {
		std::cout << Dog{} << std::endl;
	}
	
    save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-06 14:31:00.0072220
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 6일														(5주 2일)
//------------------------------------------------------------------------------------------------------- 
// 예제
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <print>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };	// printable ASCII 코드의 전 범위

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}

	bool operator()(Dog a, Dog b) {
		return a.id < b.id;
	}
private:
	std::string name;	// [1,150]
	int id;				// [0, 999'9999]

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:7}] - {}", dog.id, dog.name);	// 콘솔 말고 os에 출력
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// std::sort를 사용하여 id 기준 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서부터 1000개의 내용을 cout으로 출력하라.

std::array<Dog, 10'0000> dogs;	// 선언하는 순간 자동으로 생성자 호출.

// ----------
int main()
// ----------
{
	std::sort(dogs.begin(), dogs.end(), Dog());

	for (int i = 0; i < 1000; ++i) 
		std::cout << dogs[i] << "\n";

    save("메인.cpp"); // 내 답변
}

==========================================================================================
저장한 시간 - 2026-04-06 14:41:33.0654719
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 6일														(5주 2일)
//------------------------------------------------------------------------------------------------------- 
// 예제
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <print>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };	// printable ASCII 코드의 전 범위

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}

	bool operator()(Dog a, Dog b) {
		return a.id < b.id;
	}
private:
	std::string name;	// [1,150]
	int id;				// [0, 999'9999]

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:7}] - {}", dog.id, dog.name);	// 콘솔 말고 os에 출력
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// std::sort를 사용하여 id 기준 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서부터 1000개의 내용을 cout으로 출력하라.

std::array < Dog, 10'0000> dogs;	// free-store에 저장. (스택 오버플로우 방지)
// std::array <int, 10> a;		// 전부 0으로 자동 초기화

// ----------
int main()
// ----------
{
	// std::array < Dog, 10'0000> dogs;	// 여기에 쓰면 안됨. (스택 오버플로우 발생)
	std::array <int, 10> a;		// 처음 5개만 초기화. 나머지는 0으로 자동 초기화

	for (int num : a) {
		std::cout << num << std::endl;
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-06 14:42:21.9275889
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 6일														(5주 2일)
//------------------------------------------------------------------------------------------------------- 
// 예제
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <print>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };	// printable ASCII 코드의 전 범위

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}

	bool operator()(Dog a, Dog b) {
		return a.id < b.id;
	}
private:
	std::string name;	// [1,150]
	int id;				// [0, 999'9999]

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:7}] - {}", dog.id, dog.name);	// 콘솔 말고 os에 출력
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// std::sort를 사용하여 id 기준 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서부터 1000개의 내용을 cout으로 출력하라.

std::array < Dog, 10'0000> dogs;	// free-store에 저장. (스택 오버플로우 방지)
// std::array <int, 10> a;		// 전부 0으로 자동 초기화

// ----------
int main()
// ----------
{
	std::array < Dog, 1000> dogs;	// 클래스는 다름. 여기에 써도 자동 초기화.

	for (Dog num : dogs) {
		std::cout << num << std::endl;
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-06 14:50:40.0692264
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 6일														(5주 2일)
//------------------------------------------------------------------------------------------------------- 
// 예제
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <print>
#include <ranges>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };	// printable ASCII 코드의 전 범위

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}
	int getId() const { return id; }	// id를 반환하는 멤버 함수 (interface 함수)

private:
	std::string name;	// [1,150]
	int id;				// [0, 999'9999]

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:7}] - {}", dog.id, dog.name);	// 콘솔 말고 os에 출력
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// std::sort를 사용하여 id 기준 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서부터 1000개의 내용을 cout으로 출력하라.

std::array < Dog, 10'0000> dogs;	// free-store에 저장. (스택 오버플로우 방지)
// 전역 데이터가 만들어지는 시간은 잴 방도가 없음. 

// ----------
int main()
// ----------
{
	std::sort(dogs.begin(), dogs.end(), [](Dog a, Dog b) {		// 쓸데없는 복사 생성 -> 0점짜리 코드 (시간 오래걸림)
		return a.getId() < b.getId();
		});	// 기본으로 정렬 불가능 (operator<이 없으므로). operator()로 비교하는 객체를 전달해야 함. 
			// 무조건 람다 써서 코드 "인라인화" 하는 게 좋다.

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-06 14:51:27.9568251
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 6일														(5주 2일)
//------------------------------------------------------------------------------------------------------- 
// 예제
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <print>
#include <ranges>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };	// printable ASCII 코드의 전 범위

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}
	int getId() const { return id; }	// id를 반환하는 멤버 함수 (interface 함수)

private:
	std::string name;	// [1,150]
	int id;				// [0, 999'9999]

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:7}] - {}", dog.id, dog.name);	// 콘솔 말고 os에 출력
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// std::sort를 사용하여 id 기준 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서부터 1000개의 내용을 cout으로 출력하라.

std::array < Dog, 10'0000> dogs;	// free-store에 저장. (스택 오버플로우 방지)
// 전역 데이터가 만들어지는 시간은 잴 방도가 없음. 

// ----------
int main()
// ----------
{
	std::sort(dogs.begin(), dogs.end(), [](const Dog& a, const Dog& b) {		// 쓸데없는 복사 생성 -> 0점짜리 코드 (시간 오래걸림)
		return a.getId() < b.getId();
		});	// 기본으로 정렬 불가능 (operator<이 없으므로). operator()로 비교하는 객체를 전달해야 함. 
			// 무조건 람다 써서 코드 "인라인화" 하는 게 좋다.

	for (const Dog& dog : dogs)
		std::cout << dog << '\n';

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-06 14:53:11.8077282
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 6일														(5주 2일)
//------------------------------------------------------------------------------------------------------- 
// 예제
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <print>
#include <ranges>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };	// printable ASCII 코드의 전 범위

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}
	int getId() const { return id; }	// id를 반환하는 멤버 함수 (interface 함수)

private:
	std::string name;	// [1,150]
	int id;				// [0, 999'9999]

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:7}] - {}", dog.id, dog.name);	// 콘솔 말고 os에 출력
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// std::sort를 사용하여 id 기준 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서부터 1000개의 내용을 cout으로 출력하라.

std::array < Dog, 10'0000> dogs;	// free-store에 저장. (스택 오버플로우 방지)
// 전역 데이터가 만들어지는 시간은 잴 방도가 없음. 

// ----------
int main()
// ----------
{
	std::sort(dogs.begin(), dogs.end(), [](const Dog& a, const Dog& b) {		// 쓸데없는 복사 생성 -> 0점짜리 코드 (시간 오래걸림)
		return a.getId() < b.getId();
		});	// 기본으로 정렬 불가능 (operator<이 없으므로). operator()로 비교하는 객체를 전달해야 함. 
			// 무조건 람다 써서 코드 "인라인화" 하는 게 좋다.

	for (const Dog& dog : dogs | std::views::take(1000))
		std::cout << dog << '\n';

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-06 14:57:52.9325901
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 6일														(5주 2일)
//------------------------------------------------------------------------------------------------------- 
// 예제
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <print>
#include <ranges>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };	// printable ASCII 코드의 전 범위

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}
	int getId() const { return id; }	// id를 반환하는 멤버 함수 (interface 함수)

private:
	std::string name;	// [1,150]
	int id;				// [0, 999'9999]

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:7}] - {}", dog.id, dog.name);	// 콘솔 말고 os에 출력
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// std::sort를 사용하여 id 기준 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서부터 1000개의 내용을 cout으로 출력하라.

std::array < Dog, 10'0000> dogs;	// free-store에 저장. (스택 오버플로우 방지)
// 전역 데이터가 만들어지는 시간은 잴 방도가 없음. 

// ----------
int main()
// ----------
{
	//std::sort(dogs.begin(), dogs.end(), [](const Dog& a, const Dog& b) {		// 쓸데없는 복사 생성 -> 0점짜리 코드 (시간 오래걸림)
	//	return a.getId() < b.getId();
	//	});	// 기본으로 정렬 불가능 (operator<이 없으므로). operator()로 비교하는 객체를 전달해야 함. 
	//		// 무조건 람다 써서 코드 "인라인화" 하는 게 좋다.

	// C++20의 sort
	std::ranges::sort(dogs, {}, &Dog::getId);		// 멤버함수는 callable이 아니기 때문에 &를 붙여 주소 전달.

	for (const Dog& dog : dogs | std::views::take(1000))
		std::cout << dog << '\n';

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-06 15:01:13.9433481
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 6일														(5주 2일)
//------------------------------------------------------------------------------------------------------- 
// 예제
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <print>
#include <ranges>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };	// printable ASCII 코드의 전 범위

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}
	int getId() const { return id; }	// id를 반환하는 멤버 함수 (interface 함수)
	int getNameLength() const { return name.length(); }	// name의 길이를 반환하는 멤버 함수 (interface 함수)

private:
	std::string name;	// [1,150]
	int id;				// [0, 999'9999]

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:7}] - {}", dog.id, dog.name);	// 콘솔 말고 os에 출력
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// std::sort를 사용하여 name의 길이기준 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서부터 1000개의 내용을 cout으로 출력하라.

std::array < Dog, 10'0000> dogs;

// ----------
int main()
// ----------
{
	// C++20의 sort
	//std::ranges::sort(dogs, {}, &Dog::getId);		// 멤버함수는 callable이 아니기 때문에 &를 붙여 주소 전달.
	
	std::sort(dogs.begin(), dogs.end(), [](const Dog& a, const Dog& b) {
		return a.getNameLength() < b.getNameLength();
		});

	for (const Dog& dog : dogs | std::views::take(1000))
		std::cout << dog << '\n';

	save("메인.cpp"); // 내 답변
}

==========================================================================================
저장한 시간 - 2026-04-06 15:04:32.5303696
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 6일														(5주 2일)
//------------------------------------------------------------------------------------------------------- 
// 예제
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <print>
#include <ranges>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };	// printable ASCII 코드의 전 범위

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}
	int getId() const { return id; }	// id를 반환하는 멤버 함수 (interface 함수)
	std::string getName() const { return name; }	// 이건 과한거다. 멤버가 id랑 name이면 그 두개만 get함수 만들어야지 
	// name의 길이까지 interface함수를 만들 필요가 전혀 없다.

private:
	std::string name;	// [1,150]
	int id;				// [0, 999'9999]

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:7}] - {}", dog.id, dog.name);	// 콘솔 말고 os에 출력
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// std::sort를 사용하여 name의 길이기준 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서부터 1000개의 내용을 cout으로 출력하라.

std::array < Dog, 10'0000> dogs;

// ----------
int main()
// ----------
{
	// C++20의 sort
	//std::ranges::sort(dogs, {}, &Dog::getId);		// 멤버함수는 callable이 아니기 때문에 &를 붙여 주소 전달.
	
	std::sort(dogs.begin(), dogs.end(), [](const Dog& a, const Dog& b) {
		return a.getName().size() < b.getName().size();
		});

	for (const Dog& dog : dogs)
		std::cout << dog << '\n';

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-06 15:13:56.5260370
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 6일														(5주 2일)
//------------------------------------------------------------------------------------------------------- 
// 예제
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <print>
#include <ranges>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };	// printable ASCII 코드의 전 범위

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}
	int getId() const { return id; }	// id를 반환하는 멤버 함수 (interface 함수)
	std::string getName() const { return name; } // 이건 가짜 (수정 불가)
	std::string& getName() { return name; }	// 이게 진짜 (수정 가능)

private:
	std::string name;	// [1,150]
	int id;				// [0, 999'9999]

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:7}] - {}", dog.id, dog.name);	// 콘솔 말고 os에 출력
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// 각 Dog 객체의 이름을 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서부터 1000개의 내용을 cout으로 출력하라.

std::array < Dog, 10'0000> dogs;

// ----------
int main()
// ----------
{
	for (Dog dog : dogs) {
		std::string& name = dog.getName();
		std::sort(name.begin(), name.end());
	}

	for (const Dog& dog : dogs | std::views::take(1000))
		std::cout << dog << '\n';

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-06 15:14:34.4552670
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 6일														(5주 2일)
//------------------------------------------------------------------------------------------------------- 
// 예제
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <print>
#include <ranges>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };	// printable ASCII 코드의 전 범위

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}
	int getId() const { return id; }	// id를 반환하는 멤버 함수 (interface 함수)
	std::string getName() const { return name; } // 이건 가짜 (수정 불가)
	std::string& getName() { return name; }	// 이게 진짜 (수정 가능)

private:
	std::string name;	// [1,150]
	int id;				// [0, 999'9999]

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:7}] - {}", dog.id, dog.name);	// 콘솔 말고 os에 출력
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// 각 Dog 객체의 이름을 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서부터 1000개의 내용을 cout으로 출력하라.

std::array < Dog, 10'0000> dogs;

// ----------
int main()
// ----------
{
	for (Dog& dog : dogs) {		// &안붙이면 원본이 아닌 복사본이 만들어져서 이름이 정렬되지 않음
		std::string& name = dog.getName();
		std::sort(name.begin(), name.end());
	}

	for (const Dog& dog : dogs | std::views::take(1000))
		std::cout << dog << '\n';

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-06 15:17:55.1222283
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 6일														(5주 2일)
//------------------------------------------------------------------------------------------------------- 
// 예제
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <print>
#include <ranges>
#include <fstream>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 999'9999 };
std::uniform_int_distribution uidNameLen{ 1, 16 };
std::uniform_int_distribution<> uidChar{ '!', '~' };	// printable ASCII 코드의 전 범위

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}
	int getId() const { return id; }	// id를 반환하는 멤버 함수 (interface 함수)
	std::string getName() const { return name; } // 이건 가짜 (수정 불가)
	std::string& getName() { return name; }	// 이게 진짜 (수정 가능)

private:
	std::string name;	// [1,16]
	int id;				// [0, 999'9999]

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:7}] - {}", dog.id, dog.name);	// 콘솔 말고 os에 출력
		return os;
	}
};

std::array < Dog, 10'0000> dogs;

// ----------
int main()
// ----------
{
	std::ofstream out{ "Dog 십만마리", std::ios::binary };
	out.write((char*)dogs.data(), dogs.size() * sizeof(Dog));

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-07 16:09:08.3225854
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 7일														(6주 1일)
//------------------------------------------------------------------------------------------------------- 
// 중간고사 4월 21일 (8주 1일)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include "save.h"

class Dog {
private:
	std::string name;	// [1,16]
	int id;				// [0, 999'9999]

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:7}] - {}", dog.id, dog.name);	// 콘솔 말고 os에 출력
		return os;
	}
};

std::array<Dog, 10'0000> dogs;	// -> DATA 영역에 생성 (모두 0으로 초기화)

// [문제] 다운받은 "Dog 십만마리" 에는 class Dog 객체 십만개가 저장되어 있다. (중간고사 1번 문제)
// 바이너리 모드로 저장하여 정확하게 4MB이다.
// 메모리로 모두 읽어와라.
// 앞에서부터 100개 출력하여 확인하라.

// ----------
int main()
// ----------
{
	std::array<Dog, 1000> dogs; // -> STACK 영역에 생성 (스택 오버플로우 발생)
	for (const Dog& dog : dogs)
		std::cout << dog << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-07 16:10:07.9332422
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 7일														(6주 1일)
//------------------------------------------------------------------------------------------------------- 
// 중간고사 4월 21일 (8주 1일)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include "save.h"

class Dog {
private:
	std::string name;	// [1,16]
	int id;				// [0, 999'9999]

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:7}] - {}", dog.id, dog.name);	// 콘솔 말고 os에 출력
		return os;
	}
};

std::array<Dog, 10'0000> dogs;	// -> DATA 영역에 생성 (모두 0으로 초기화)

// [문제] 다운받은 "Dog 십만마리" 에는 class Dog 객체 십만개가 저장되어 있다. (중간고사 1번 문제)
// 바이너리 모드로 저장하여 정확하게 4MB이다.
// 메모리로 모두 읽어와라.
// 앞에서부터 100개 출력하여 확인하라.

// ----------
int main()
// ----------
{
	std::array<Dog, 1000> dogs; // -> STACK 영역에 생성 (스택 오버플로우 발생) -> 1000개만 해서 값 확인해보기
	for (const Dog& dog : dogs)
		std::cout << dog << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-07 16:16:32.2096923
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 7일														(6주 1일)
//------------------------------------------------------------------------------------------------------- 
// 중간고사 4월 21일 (8주 1일)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <fstream>
#include "save.h"

class Dog {
private:
	std::string name;	// [1,16]		-> 문제 발생하는 부분
	int id;				// [0, 999'9999]

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:7}] - {}", dog.id, dog.name);	// 콘솔 말고 os에 출력
		return os;
	}
};

std::array<Dog, 10'0000> dogs;	// -> DATA 영역에 생성 (모두 0으로 초기화)

// [문제] 다운받은 "Dog 십만마리" 에는 class Dog 객체 십만개가 저장되어 있다. (중간고사 1번 문제)
// 바이너리 모드로 저장하여 정확하게 4MB이다.
// 메모리로 모두 읽어와라.
// 앞에서부터 100개 출력하여 확인하라.
// 
// 정답 : 이런 쓰레기 문제는 풀 수 없다.

// ----------
int main()
// ----------
{
	save("메인.cpp");
	std::ifstream in{ "Dog 십만마리", std::ios::binary };
	if (not in) {
		std::cout << "파일을 살펴보세요" << std::endl;
		return 20260407;
	}
	// std::array<Dog, 10'0000> dogs; // -> STACK 영역에 생성 (스택 오버플로우 발생) -> 1000개만 해서 값 확인해보기

	in.read((char*)(dogs.data()), dogs.size() * sizeof(Dog));

	for (int i = 0; i < 100; ++i)
		std::cout << dogs[i] << std::endl;

	/*for (const Dog& dog : dogs)
		std::cout << dog << std::endl;*/

	
}

==========================================================================================
저장한 시간 - 2026-04-07 16:36:21.4511723
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 7일														(6주 1일)
//------------------------------------------------------------------------------------------------------- 
// 중간고사 4월 21일 (8주 1일)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <fstream>
#include "save.h"

class Dog {
private:
	std::string name;	// [1,16]		-> 문제 발생하는 부분
	int id;				// [0, 999'9999]

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		std::print(os, "[{:7}] - {}", dog.id, dog.name);	// 콘솔 말고 os에 출력
		return os;
	}
};

std::array<Dog, 10'0000> dogs;	// -> DATA 영역에 생성 (모두 0으로 초기화)

// [문제] 다운받은 "Dog 십만마리" 에는 class Dog 객체 십만개가 저장되어 있다. (중간고사 1번 문제)
// 바이너리 모드로 저장하여 정확하게 4MB이다.
// 메모리로 모두 읽어와라.
// 앞에서부터 100개 출력하여 확인하라.
// 
// 정답 : 이런 쓰레기 문제는 풀 수 없다.

// ----------
int main()
// ----------
{
	save("메인.cpp");
	std::ifstream in{ "Dog 십만마리", std::ios::binary };
	if (not in) {
		std::cout << "파일을 살펴보세요" << std::endl;
		return 20260407;
	}
	// std::array<Dog, 10'0000> dogs; // -> STACK 영역에 생성 (스택 오버플로우 발생) -> 1000개만 해서 값 확인해보기

	in.read((char*)(dogs.data()), dogs.size() * sizeof(Dog));

	for (int i = 0; i < 100; ++i)
		std::cout << dogs[i] << std::endl;

	/*for (const Dog& dog : dogs)
		std::cout << dog << std::endl;*/


}

==========================================================================================
저장한 시간 - 2026-04-07 16:40:57.4845216
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 7일														(6주 1일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString 클래스
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// ----------
int main()
// ----------
{
	//char* s = "Hello, World!";
	//string s = "Hello, World!";
	//string s{ " Hello, World!" };
	// 순으로 발전
	std::string s{ "2026" };
	s += "0407";
	std::cout << s << std::endl;

	for (auto i{ s.begin() }; i < s.end(); ++i)
		std::cout << *i << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-07 16:41:51.5888937
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 7일														(6주 1일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString 클래스
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

// ----------
int main()
// ----------
{
	//char* s = "Hello, World!";
	//string s = "Hello, World!";
	//string s{ " Hello, World!" };
	// 순으로 발전
	std::string s{ "2026" };
	s += "0407";
	std::cout << s << std::endl;

	for (auto i{ s.rbegin() }; i < s.rend(); ++i)
		std::cout << *i << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-07 16:54:40.2375036
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 7일														(6주 1일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include "save.h"

class ZString {
public:
	ZString() = default;

	ZString(const char* s) {
		len = std::strlen(s);
		p = std::make_unique<char[]>(len);	// 스마트 포인터는 원래 만들어짐과 동시에 초기화되어야 하지만 그게 아니라면 이렇게 초기화
		memcpy(p.get(), s, len);		// 이거보다 빠른 명령어는 없다. 제일 밑바닥 (어셈블리 수준)
	}

	friend std::ostream& operator<<(std::ostream& os, const ZString& zs) {
		for (int i = 0; i < zs.len; ++i)
			os << *(zs.p.get() + i);		// 유니크 포인터가 확보한 메모리는 p.get()으로 알 수 있음.
		return os;
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};
};

// ----------
int main()
// ----------
{
	// std::string s{ "2026" };
	// 그럼 만약 처음에 초기화할 땐 15글자 이하였다가 추가해서 16글자 이상이 되면 저장하는 위치도 달라지나?

	ZString s{ "2026" };
	std::cout << s << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-07 16:58:01.4625922
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 7일														(6주 1일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include "save.h"

class ZString {
public:
	ZString() = default;

	ZString(const char* s) {
		len = std::strlen(s);
		p = std::make_unique<char[]>(len);	// 스마트 포인터는 원래 만들어짐과 동시에 초기화되어야 하지만 그게 아니라면 이렇게 초기화
		memcpy(p.get(), s, len);		// 이거보다 빠른 명령어는 없다. 제일 밑바닥 (어셈블리 수준)

		std::cout << "생성(char*) 글자수: " << len << " 객체: " << this << " 글자주소: " << p.get() << std::endl;
	}

	friend std::ostream& operator<<(std::ostream& os, const ZString& zs) {
		for (int i = 0; i < zs.len; ++i)
			os << *(zs.p.get() + i);		// 유니크 포인터가 확보한 메모리는 p.get()으로 알 수 있음.
		return os;
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};
};

// ----------
int main()
// ----------
{
	ZString s{ "2026" };
	std::cout << s << std::endl;

	save("메인.cpp");
}

// 질문
// std::string에서 15글자 이하일 땐 2칸에 메모리 낭비, 16글자 이상일 땐 3~4칸에 메모리 낭비되는지
// 그럼 만약 처음에 초기화할 땐 15글자 이하였다가 추가해서 16글자 이상이 되면 저장하는 위치도 달라지나?

==========================================================================================
저장한 시간 - 2026-04-07 17:00:11.0603497
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 7일														(6주 1일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include "save.h"

class ZString {
public:
	ZString() = default;

	ZString(const char* s) {
		len = std::strlen(s);
		p = std::make_unique<char[]>(len);	// 스마트 포인터는 원래 만들어짐과 동시에 초기화되어야 하지만 그게 아니라면 이렇게 초기화
		memcpy(p.get(), s, len);		// 이거보다 빠른 명령어는 없다. 제일 밑바닥 (어셈블리 수준)

		std::cout << "생성(char*) 글자수: " << len << " 객체: " << this << " 글자주소: " << (void*)p.get() << std::endl;
		// p.get()은 주소가 틀림없다. 근데 << 가 고급 그거라 char* 일 때는 이 주소로부터 적혀있는 걸 
		// 바이트단위로 읽어다가 아스키 코드로 찍어준다. 널 캐릭터를 만날때까지.
	}

	friend std::ostream& operator<<(std::ostream& os, const ZString& zs) {
		for (int i = 0; i < zs.len; ++i)
			os << *(zs.p.get() + i);		// 유니크 포인터가 확보한 메모리는 p.get()으로 알 수 있음.
		return os;
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};
};

// ----------
int main()
// ----------
{
	ZString s{ "2026" };
	std::cout << s << std::endl;

	save("메인.cpp");
}

// 질문
// std::string에서 15글자 이하일 땐 2칸에 메모리 낭비, 16글자 이상일 땐 3~4칸에 메모리 낭비되는지
// 그럼 만약 처음에 초기화할 땐 15글자 이하였다가 추가해서 16글자 이상이 되면 저장하는 위치도 달라지나?

==========================================================================================
저장한 시간 - 2026-04-07 17:02:52.7312445
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 7일														(6주 1일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include "save.h"

class ZString {
public:
	ZString() {
		std::cout << "생성() 글자수: " << len << " 객체: " << this << " 글자주소: " << (void*)p.get() << std::endl;
	}

	ZString(const char* s) {
		len = std::strlen(s);
		p = std::make_unique<char[]>(len);	// 스마트 포인터는 원래 만들어짐과 동시에 초기화되어야 하지만 그게 아니라면 이렇게 초기화
		memcpy(p.get(), s, len);		// 이거보다 빠른 명령어는 없다. 제일 밑바닥 (어셈블리 수준)

		std::cout << "생성(char*) 글자수: " << len << " 객체: " << this << " 글자주소: " << (void*)p.get() << std::endl;
		// p.get()은 주소가 틀림없다. 근데 << 가 고급 그거라 char* 일 때는 이 주소로부터 적혀있는 걸 
		// 바이트단위로 읽어다가 아스키 코드로 찍어준다. 널 캐릭터를 만날때까지.
		// 객체는 스택(저주소), 글자는 free-store(고주소) 에 저장
	}

	friend std::ostream& operator<<(std::ostream& os, const ZString& zs) {
		for (int i = 0; i < zs.len; ++i)
			os << *(zs.p.get() + i);		// 유니크 포인터가 확보한 메모리는 p.get()으로 알 수 있음.
		return os;
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};
};

// ----------
int main()
// ----------
{
	ZString{};	// 이름없는 객체도 생성된다.

	ZString s{ "2026" };
	std::cout << s << std::endl;

	save("메인.cpp");
}

// 질문
// std::string에서 15글자 이하일 땐 2칸에 메모리 낭비, 16글자 이상일 땐 3~4칸에 메모리 낭비되는지
// 그럼 만약 처음에 초기화할 땐 15글자 이하였다가 추가해서 16글자 이상이 되면 저장하는 위치도 달라지나?

==========================================================================================
저장한 시간 - 2026-04-07 17:12:45.0715900
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 7일														(6주 1일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include "save.h"

class ZString {
public:
	ZString() {
		std::cout << "생성() 글자수: " << len << " 객체: " << this << " 글자주소: " << (void*)p.get() << std::endl;
	}

	ZString(const char* s) {
		len = std::strlen(s);
		p = std::make_unique<char[]>(len);	// 스마트 포인터는 원래 만들어짐과 동시에 초기화되어야 하지만 그게 아니라면 이렇게 초기화
		memcpy(p.get(), s, len);		// 이거보다 빠른 명령어는 없다. 제일 밑바닥 (어셈블리 수준)

		std::cout << "생성(char*) 글자수: " << len << " 객체: " << this << " 글자주소: " << (void*)p.get() << std::endl;
		// p.get()은 주소가 틀림없다. 근데 << 가 고급 그거라 char* 일 때는 이 주소로부터 적혀있는 걸 
		// 바이트단위로 읽어다가 아스키 코드로 찍어준다. 널 캐릭터를 만날때까지.
		// 객체는 스택(저주소), 글자는 free-store(고주소) 에 저장
	}

	friend std::ostream& operator<<(std::ostream& os, const ZString& zs) {
		for (int i = 0; i < zs.len; ++i)
			os << *(zs.p.get() + i);		// 유니크 포인터가 확보한 메모리는 p.get()으로 알 수 있음.
		return os;
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};
};

// ----------
int main()
// ----------
{
	std::string s{ "2026년" };
	std::string t{ "4월 7일 화요일" };

	std::string u = s + t;
	std::cout << u << std::endl;

	save("메인.cpp");
}

// 질문
// std::string에서 15글자 이하일 땐 2칸에 메모리 낭비, 16글자 이상일 땐 3~4칸에 메모리 낭비되는지
// 그럼 만약 처음에 초기화할 땐 15글자 이하였다가 추가해서 16글자 이상이 되면 저장하는 위치도 달라지나?

==========================================================================================
저장한 시간 - 2026-04-07 17:13:54.9023431
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 7일														(6주 1일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include "save.h"

class ZString {
public:
	ZString() {
		std::cout << "생성() 글자수: " << len << " 객체: " << this << " 글자주소: " << (void*)p.get() << std::endl;
	}

	ZString(const char* s) {
		len = std::strlen(s);
		p = std::make_unique<char[]>(len);	// 스마트 포인터는 원래 만들어짐과 동시에 초기화되어야 하지만 그게 아니라면 이렇게 초기화
		memcpy(p.get(), s, len);		// 이거보다 빠른 명령어는 없다. 제일 밑바닥 (어셈블리 수준)

		std::cout << "생성(char*) 글자수: " << len << " 객체: " << this << " 글자주소: " << (void*)p.get() << std::endl;
		// p.get()은 주소가 틀림없다. 근데 << 가 고급 그거라 char* 일 때는 이 주소로부터 적혀있는 걸 
		// 바이트단위로 읽어다가 아스키 코드로 찍어준다. 널 캐릭터를 만날때까지.
		// 객체는 스택(저주소), 글자는 free-store(고주소) 에 저장
	}

	friend std::ostream& operator<<(std::ostream& os, const ZString& zs) {
		for (int i = 0; i < zs.len; ++i)
			os << *(zs.p.get() + i);		// 유니크 포인터가 확보한 메모리는 p.get()으로 알 수 있음.
		return os;
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};
};

// ----------
int main()
// ----------
{
	std::string s{ "2026년" };
	std::string t{ "4월7일 화요일" };

	std::string u = s + t;		// concatenation
	std::cout << u << std::endl;

	save("메인.cpp");
}

// 질문
// std::string에서 15글자 이하일 땐 2칸에 메모리 낭비, 16글자 이상일 땐 3~4칸에 메모리 낭비되는지
// 그럼 만약 처음에 초기화할 땐 15글자 이하였다가 추가해서 16글자 이상이 되면 저장하는 위치도 달라지나?

==========================================================================================
저장한 시간 - 2026-04-07 17:14:47.3839552
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 7일														(6주 1일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include "save.h"

class ZString {
public:
	ZString() {
		std::cout << "생성() 글자수: " << len << " 객체: " << this << " 글자주소: " << (void*)p.get() << std::endl;
	}

	ZString(const char* s) {
		len = std::strlen(s);
		p = std::make_unique<char[]>(len);	// 스마트 포인터는 원래 만들어짐과 동시에 초기화되어야 하지만 그게 아니라면 이렇게 초기화
		memcpy(p.get(), s, len);		// 이거보다 빠른 명령어는 없다. 제일 밑바닥 (어셈블리 수준)

		std::cout << "생성(char*) 글자수: " << len << " 객체: " << this << " 글자주소: " << (void*)p.get() << std::endl;
		// p.get()은 주소가 틀림없다. 근데 << 가 고급 그거라 char* 일 때는 이 주소로부터 적혀있는 걸 
		// 바이트단위로 읽어다가 아스키 코드로 찍어준다. 널 캐릭터를 만날때까지.
		// 객체는 스택(저주소), 글자는 free-store(고주소) 에 저장
	}

	friend std::ostream& operator<<(std::ostream& os, const ZString& zs) {
		for (int i = 0; i < zs.len; ++i)
			os << *(zs.p.get() + i);		// 유니크 포인터가 확보한 메모리는 p.get()으로 알 수 있음.
		return os;
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};
};

// ----------
int main()
// ----------
{
	std::string s{ "2026년" };
	std::string t = move(s);

	std::cout << "s - " << s << std::endl;
	std::cout << "t - " << t << std::endl;

	save("메인.cpp");
}

// 질문
// std::string에서 15글자 이하일 땐 2칸에 메모리 낭비, 16글자 이상일 땐 3~4칸에 메모리 낭비되는지
// 그럼 만약 처음에 초기화할 땐 15글자 이하였다가 추가해서 16글자 이상이 되면 저장하는 위치도 달라지나?

==========================================================================================
저장한 시간 - 2026-04-07 17:20:07.6337119
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 7일														(6주 1일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include "save.h"

class ZString {
public:
	ZString() {
		std::cout << "생성() 글자수: " << len << " 객체: " << this << " 글자주소: " << (void*)p.get() << std::endl;
	}

	ZString(const char* s) {
		len = std::strlen(s);
		p = std::make_unique<char[]>(len);	// 스마트 포인터는 원래 만들어짐과 동시에 초기화되어야 하지만 그게 아니라면 이렇게 초기화
		memcpy(p.get(), s, len);		// 이거보다 빠른 명령어는 없다. 제일 밑바닥 (어셈블리 수준)

		std::cout << "생성(char*) 글자수: " << len << " 객체: " << this << " 글자주소: " << (void*)p.get() << std::endl;
		// p.get()은 주소가 틀림없다. 근데 << 가 고급 그거라 char* 일 때는 이 주소로부터 적혀있는 걸 
		// 바이트단위로 읽어다가 아스키 코드로 찍어준다. 널 캐릭터를 만날때까지.
		// 객체는 스택(저주소), 글자는 free-store(고주소) 에 저장
	}

	friend std::ostream& operator<<(std::ostream& os, const ZString& zs) {
		for (int i = 0; i < zs.len; ++i)
			os << *(zs.p.get() + i);		// 유니크 포인터가 확보한 메모리는 p.get()으로 알 수 있음.
		return os;
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};
};

// ----------
int main()
// ----------
{
	// 이 동작 다시 설명하고 ZString으로 바꾸면 안되는 이유부터 시작
	std::string s{ "2026년" };		// xvalue , t는 lvalue
	std::string t = move(s);		// -> t = static_cast<std::string&&>(s); -> t {static_cast<std::string&&>(s)}; -> t.string(string&&);
	// move라는 건 존재하지 않고 그냥 타입 캐스팅	( && -> r-value reference )

	std::cout << "s - " << s << std::endl;
	std::cout << "t - " << t << std::endl;

	save("메인.cpp");
}

// 질문
// std::string에서 15글자 이하일 땐 2칸에 메모리 낭비, 16글자 이상일 땐 3~4칸에 메모리 낭비되는지
// 그럼 만약 처음에 초기화할 땐 15글자 이하였다가 추가해서 16글자 이상이 되면 저장하는 위치도 달라지나?

==========================================================================================
저장한 시간 - 2026-04-13 14:10:50.5913089
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 13일													(6주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include "save.h"
#include "ZString.h"

extern bool 관찰;

// ----------
int main()
// ----------
{
	관찰 = true;

	ZString zs;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-13 14:12:24.7005434
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 13일													(6주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include "save.h"
#include "ZString.h"

extern bool 관찰;

// ----------
int main()
// ----------
{
	관찰 = true;
	
	ZString zs[5]{"333", "1", "55555", "4444", "22" };

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-13 14:14:03.4701363
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 13일													(6주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include "save.h"
#include "ZString.h"

extern bool 관찰;

// ----------
int main()
// ----------
{
	관찰 = true;
	
	ZString zs[5]{"333", "1", "55555", "4444", "22" };

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-13 14:35:49.0968154
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 13일													(6주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include "save.h"
#include "ZString.h"

extern bool 관찰;

// ----------
int main()
// ----------
{
	관찰 = true;
	
	// ZString zs[5]{"333", "1", "55555", "4444", "22" };

	ZString a{ "2026년 4월 13일" };
	ZString b = a;		// 복사 생성

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-13 14:38:28.3441182
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 13일													(6주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include <array>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	std::array<ZString, 5> zs {"333", "1", "55555", "4444", "22" };

	for (ZString a : zs)
		std::cout << a << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-13 14:39:17.3305141
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 13일													(6주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include <array>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	std::array<ZString, 5> zs {"333", "1", "55555", "4444", "22" };

	관찰 = true;		// 왜 0점짜리 코드인지 확인
	for (ZString a : zs)
		std::cout << a << std::endl;
	관찰 = false;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-13 14:50:54.2144755
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 13일													(6주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include <array>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	std::array<ZString, 5> zs {"333", "1", "55555", "4444", "22" };

	관찰 = true;		// 왜 0점짜리 코드인지 확인
	for (ZString& a : zs)		// 복사 생성하지말고 참조로 받아야 함 (복사 생성하면 5개짜리 배열이 10개짜리 배열이 됨)
		std::cout << a << std::endl;
	관찰 = false;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-13 14:55:05.6026992
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 13일													(6주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include <array>
#include <algorithm>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	std::array<ZString, 5> zs {"333", "1", "55555", "4444", "22" };

	// [문제] zs를 길이 오름차순으로 정렬하라.
	std::sort(zs.begin(), zs.end(), [](ZString a, ZString b) {
		return a.getLen() < b.getLen();
		});

	for (const ZString& s : zs)
		std::cout << s << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-13 14:55:34.7901723
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 13일													(6주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include <array>
#include <algorithm>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	std::array<ZString, 5> zs {"333", "1", "55555", "4444", "22" };

	// [문제] zs를 길이 오름차순으로 정렬하라. (같은 이유로 0점짜리 코드)
	관찰 = true;
	std::sort(zs.begin(), zs.end(), [](ZString a, ZString b) {
		return a.getLen() < b.getLen();
		});
	관찰 = false;

	for (const ZString& s : zs)
		std::cout << s << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-13 14:55:52.6761767
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 13일													(6주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include <array>
#include <algorithm>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	std::array<ZString, 5> zs {"333", "1", "55555", "4444", "22" };

	// [문제] zs를 길이 오름차순으로 정렬하라. (같은 이유로 0점짜리 코드)
	관찰 = true;
	std::sort(zs.begin(), zs.end(), [](ZString& a, ZString& b) {
		return a.getLen() < b.getLen();
		});
	관찰 = false;

	for (const ZString& s : zs)
		std::cout << s << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-13 14:56:18.8619680
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 13일													(6주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include <array>
#include <algorithm>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	std::array<ZString, 5> zs {"333", "1", "55555", "4444", "22" };

	// [문제] zs를 길이 오름차순으로 정렬하라. (같은 이유로 0점짜리 코드)
	관찰 = true;
	std::sort(zs.begin(), zs.end(), [](const ZString& a, const ZString& b) {
		return a.getLen() < b.getLen();
		});
	관찰 = false;

	for (const ZString& s : zs)
		std::cout << s << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-13 14:57:54.1277593
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 13일													(6주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include <array>
#include <algorithm>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	std::array<ZString, 5> zs {"333", "1", "55555", "4444", "22" };

	// [문제] zs를 길이 오름차순으로 정렬하라. - C++11 이전 code
	관찰 = true;
	std::sort(zs.begin(), zs.end(), [](const ZString& a, const ZString& b) {
		return a.getLen() < b.getLen();
		});
	관찰 = false;

	for (const ZString& s : zs)
		std::cout << s << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-13 15:09:39.2588509
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 13일													(6주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include <array>
#include <algorithm>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	std::array<ZString, 5> zs {"333", "1", "55555", "4444", "22" };

	// [문제] zs를 길이 오름차순으로 정렬하라. - C++11 이전 code
	관찰 = true;
	std::sort(zs.begin(), zs.end(), [](const ZString& a, const ZString& b) {
		return a.getLen() < b.getLen();
		});
	관찰 = false;

	for (const ZString& s : zs)
		std::cout << s << std::endl;

	save("메인.cpp");
}

/*
[      6] 복사생성 - 객체:0X00EF2B4FFC00, 글자:0X029A66B63020, 개수:1      내용:1
[      2] 복사할당 - 객체:0X00EF2B4FFD98, 글자:0X029A66B63170, 개수:3      내용:333
[      1] 복사할당 - 객체:0X00EF2B4FFD80, 글자:0X029A66B63000, 개수:1      내용:1
[      6] 소멸     - 객체:0X00EF2B4FFC00, 글자:0X029A66B63020, 개수:1      내용:1
[      7] 복사생성 - 객체:0X00EF2B4FFC00, 글자:0X029A66B63010, 개수:5      내용:55555
[      3] 복사할당 - 객체:0X00EF2B4FFDB0, 글자:0X029A66B63150, 개수:5      내용:55555
[      7] 소멸     - 객체:0X00EF2B4FFC00, 글자:0X029A66B63010, 개수:5      내용:55555
[      8] 복사생성 - 객체:0X00EF2B4FFC00, 글자:0X029A66B63110, 개수:4      내용:4444
[      4] 복사할당 - 객체:0X00EF2B4FFDC8, 글자:0X029A66B63020, 개수:5      내용:55555
[      3] 복사할당 - 객체:0X00EF2B4FFDB0, 글자:0X029A66B63050, 개수:4      내용:4444
[      8] 소멸     - 객체:0X00EF2B4FFC00, 글자:0X029A66B63110, 개수:4      내용:4444
[      9] 복사생성 - 객체:0X00EF2B4FFC00, 글자:0X029A66B63010, 개수:2      내용:22
[      5] 복사할당 - 객체:0X00EF2B4FFDE0, 글자:0X029A66B63030, 개수:5      내용:55555
[      4] 복사할당 - 객체:0X00EF2B4FFDC8, 글자:0X029A66B63150, 개수:4      내용:4444
[      3] 복사할당 - 객체:0X00EF2B4FFDB0, 글자:0X029A66B63130, 개수:3      내용:333
[      2] 복사할당 - 객체:0X00EF2B4FFD98, 글자:0X029A66B63060, 개수:2      내용:22
[      9] 소멸     - 객체:0X00EF2B4FFC00, 글자:0X029A66B63010, 개수:2      내용:22
1
22
333
4444
55555
"메인.cpp"를 저장합니다

C:\STL_Lecture\x64\Release\STL_Lecture.exe(프로세스 30676)이(가) 0 코드(0x0)와 함께 종료되었습니다.
*/

==========================================================================================
저장한 시간 - 2026-04-13 15:19:01.0486042
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 13일													(6주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <memory>		// 스마트 포인터 사용 - 1. 유니크 포인터 (많이 씀) / 2. 쉐어드 포인터 (거의 안씀)
#include <array>
#include <fstream>
#include <algorithm>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	// [문제] "메인.cpp"에 있는 모든 단어를 ZString 객체로 읽어와라.
	// 이 문제에서 단어란 공백으로 분리된 문자집합을 말한다.
	// 다음 문장이 문제없이 실행되도록 하자.

	std::ifstream in{ "메인.cpp" };
	if (not in) {
		std::cout << "파일 없음" << std::endl;
		return 20260413;
	}

	ZString s;
	while (in >> s) {
		std::cout << s << std::endl;
	}

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-14 15:52:07.7619140
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 14일													(7주 1일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <vector>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	// 이런 코드도 가능
	std::array<ZString, 2> a{ "1", "333" };

	std::array<std::array<ZString, 2>, 10> b;
	std::vector<std::array<ZString, 2>> v;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-14 16:41:01.7565195
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 14일													(7주 1일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <vector>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	관찰 = true;
	std::array<ZString, 2> a{ "1", "333" };

	a[0] = "213214521135153151412213";

	std::cout << a[0] << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-14 16:41:53.3762446
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 14일													(7주 1일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <vector>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	관찰 = true;
	std::array<ZString, 2> a{ "1", "333" };

	a[0] = "213214521135153151412213";	// 3번째 객체가 생성(*) 되고, 그걸 1번째 객체에 이동할당 한 후 3번째 객체는 소멸.

	std::cout << a[0] << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-14 16:50:45.8107631
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 14일													(7주 1일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <vector>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	관찰 = true;
	std::array<ZString, 2> a{ "1", "333" };

	class XXX {};
	// XXX는 복사생성 가능하다. 스페셜 함수
	XXX x;
	XXX y{ x };

	std::array<XXX, 10> aa;
	// 이때 XXX는 이동생성(혹은 복사생성) 가능해야하고, 이동할당 가능해야한다.

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-14 17:03:53.2635133
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 14일													(7주 1일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <vector>
#include <list>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	관찰 = true;
	std::list<ZString> a{ "1", "333", "22", "4444", "55555"};
	관찰 = false;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-14 17:05:22.2007078
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 14일													(7주 1일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <vector>
#include <list>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{

	std::list<ZString> a{ "1", "333", "22", "4444", "55555"};

	관찰 = true;
	for (const ZString& zs : a)
		std::cout << zs << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-14 17:06:17.8620342
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 14일													(7주 1일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <vector>
#include <list>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{

	std::list<ZString> a{ "1", "333", "22", "4444", "55555"};

	관찰 = true;
	for (const ZString& zs : a)
		std::cout << zs << std::endl;

	// a.data();	// list는 contiguous한 자료구조가 아니기 때문에 시작 주소를 반환하는 data() 멤버함수가 없음.

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-14 17:13:41.4625975
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 14일													(7주 1일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	std::array<ZString, 5> a{ "1", "333", "22", "4444", "55555"};

	std::cout << "원소 수 - " << a.size() << std::endl;
	std::cout << "비었니? - " << std::boolalpha << a.empty() << std::endl;
	std::cout << "최대 수 - " << a.max_size() << std::endl;		// 이건 일관성을 위해 (원래 array는 필요없음)

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-14 17:14:21.2892176
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 14일													(7주 1일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <list>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	std::list<ZString> a{ "1", "333", "22", "4444", "55555"};

	std::cout << "원소 수 - " << a.size() << std::endl;
	std::cout << "비었니? - " << std::boolalpha << a.empty() << std::endl;
	std::cout << "최대 수 - " << a.max_size() << std::endl;		// 이건 일관성을 위해 (원래 array는 필요없음)

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-14 17:16:11.4823052
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 14일													(7주 1일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <list>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	std::array<ZString, 5> a{ "1", "333", "22", "4444", "55555"};
	a.fill("2026년 4월 14일");

	for (const ZString& zs : a)
		std::cout << zs << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-14 17:16:45.1847713
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 14일													(7주 1일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <list>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	std::array<ZString, 5> a{ "1", "333", "22", "4444", "55555"};
	
	관찰 = true;
	a.fill("2026년 4월 14일");
	관찰 = false;

	for (const ZString& zs : a)
		std::cout << zs << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-20 13:34:13.2467486
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <list>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	std::array<ZString, 5> a{ "1", "333", "22", "4444", "55555"};
	
	// 아쉽지만 다음 시간에
	// iterator로 a를 순회
	// element access - at, operator[], front, back, data

	std::cout << a[0] << std::endl;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-20 13:36:51.4942779
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <list>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	std::array<ZString, 5> a{ "1", "333", "22", "4444", "55555"};
	
	// 아쉽지만 다음 시간에
	// iterator로 a를 순회
	// element access - at, operator[], front, back, data

	관찰 = true;
	a[0].show();

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-20 13:38:23.8457943
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <list>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	std::array<ZString, 5> a{ "1", "333", "22", "4444", "55555"};
	
	// 아쉽지만 다음 시간에
	// iterator로 a를 순회
	// element access - at, operator[], front, back, data

	std::cin >> a[0];
	관찰 = true;
	a[0].show();

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-20 13:48:01.3326176
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <list>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	std::array<ZString, 5> a{ "1", "333", "22", "4444", "55555"};

	// std::array<ZString,5>::iterator i = a.begin();		// 이게 정석인데 아무도 이렇게 안 씀
	auto i = a.begin();		// 위의 주석과 똑같은 표현

	// 표준 순회 방식
	for (auto i = a.begin(); i != a.end(); ++i)		// ++i를 써야함. 이거때문에 int i 할 때도 ++i를 쓰는 거임.
		std::cout << (*i) << std::endl;
	
	// iterator로 a를 순회
	// element access - at, operator[], front, back, data

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-20 13:50:45.0866690
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <list>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	std::array<ZString, 5> a{ "1", "22", "333","4444", "55555" };

	// std::array<ZString,5>::iterator i = a.begin();		// 이게 정석인데 아무도 이렇게 안 씀
	auto i = a.begin();		// 위의 주석과 똑같은 표현

	for (auto i = a.rbegin(); i != a.rend(); ++i)		// ++i를 써야함. 이거때문에 int i 할 때도 ++i를 쓰는 거임.
		// i < a.end() 이렇게 할 수도 있는데 모든 코드에서 쓸 수 있는 건 i != a.end()이다. i < a.end()는 메모리가 contiguous하다는 게 자명하기 때문에 사용할 수 있는 것. list는 저거 안됨.
		std::cout << (*i) << std::endl;
	
	// iterator로 a를 순회
	// element access - at, operator[], front, back, data

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-20 13:54:03.1646394
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <list>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	std::array<ZString, 5> a{ "1", "22", "333","4444", "55555" };

	// element access - at, operator[], front, back, data
	관찰 = true;
	// 아래 4줄은 동일한 출력 결과
	a.begin()->show();
	a[0].show();
	a.front().show();
	a.data()->show();
	관찰 = false;

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-20 13:55:49.5862874
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <list>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	std::array<ZString, 5> a{ "1", "22", "333","4444", "55555" };

	// element access - at, operator[], front, back, data
	관찰 = true;
	(a.end()-1)->show();
	a[a.size()-1].show();
	a.back().show();
	(a.data()+a.size()-1)->show();
	관찰 = false;
	// 결국 처음 원소 접근할 때에는 front가, 마지막 원소 접근할 때에는 back이 가장 효율적.

	save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-04-20 13:58:44.2917595
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <list>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	save("메인.cpp");
	std::array<ZString, 5> a{ "1", "22", "333","4444", "55555" };

	// at()
	while (true) {
		std::cout << "찾을 원소는?";
		int num;
		std::cin >> num;
		std::cout << num << "번 - " << a[num] << std::endl;
	}
}

==========================================================================================
저장한 시간 - 2026-04-20 13:59:37.2860905
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <list>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	save("메인.cpp");
	std::array<int, 5> a{ 1, 2, 3, 4, 5 };

	// at()
	while (true) {
		std::cout << "찾을 원소는?";
		int num;
		std::cin >> num;
		std::cout << num << "번 - " << a[num] << std::endl;
	}
}

==========================================================================================
저장한 시간 - 2026-04-20 14:01:27.6540016
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <list>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	save("메인.cpp");
	// array는 지정되지 않은 곳에 접근해도 죽지 않는다 (한 10000번째 정도는 죽음)
	// 아까는 ZString이 유니크 포인터였기 때문에
	std::array<int, 5> a{ 1, 2, 3, 4, 5 };

	// at()
	while (true) {
		std::cout << "찾을 원소는?";
		int num;
		std::cin >> num;
		std::cout << num << "번 - " << *(a.data() + sizeof(int) * num) << std::endl;
	}
}

==========================================================================================
저장한 시간 - 2026-04-20 14:01:56.2847385
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <list>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	save("메인.cpp");
	// array는 지정되지 않은 곳에 접근해도 죽지 않는다 (한 10000번째 정도는 죽음)
	// 아까는 ZString이 유니크 포인터였기 때문에
	std::array<int, 5> a{ 1, 2, 3, 4, 5 };

	// at()
	while (true) {
		std::cout << "찾을 원소는?";
		int num;
		std::cin >> num;
		std::cout << num << "번 - " << *(a.data() + (sizeof(int) * num)) << std::endl;
	}
}

==========================================================================================
저장한 시간 - 2026-04-20 14:03:23.2867321
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <list>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	save("메인.cpp");
	// array는 지정되지 않은 곳에 접근해도 죽지 않는다. 우연히 죽을 순 있음.
	// 아까는 ZString이 유니크 포인터였기 때문에 죽었던 것.
	std::array<int, 5> a{ 1, 2, 3, 4, 5 };

	// at()
	while (true) {
		std::cout << "찾을 원소는?";
		int num;
		std::cin >> num;
		if (num < 0 or a.size() <= num) {
			std::cout << "다시 입력" << std::endl;
		}
		else {
			std::cout << num << "번 - " << *(a.data() + sizeof(int) * num) << std::endl;
		}
	}
	// 이렇게 접근하면 시간을 잡아먹는다. 검사하느라.. (속도에 방해되는 것들은 두지 않는다)
	// *(a.data() + sizeof(int) * num) => 꿀문법
}

==========================================================================================
저장한 시간 - 2026-04-20 14:07:57.1433328
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <list>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	save("메인.cpp");
	// array는 지정되지 않은 곳에 접근해도 죽지 않는다. 우연히 죽을 순 있음.
	// 아까는 ZString이 유니크 포인터였기 때문에 죽었던 것.
	std::array<int, 5> a{ 1, 2, 3, 4, 5 };

	// at() - 경계를 검사하고 싶다면 이 함수를 사용하면 된다.
	// C++은 속도를 최우선으로 하는 언어이다.
	// 시간걸리는 at()을 다른 함수로 제공하는 이유이다.
	// at()은 예외를 던진다.
	while (true) {
		std::cout << "찾을 원소는?";
		int num;
		std::cin >> num;
		
		try {
			std::cout << a.at(num) << std::endl;
		}
		catch (std::exception& e) {
			std::cout << e.what() << std::endl;	// 표준예외를 출력한다.
		}
	}
}

==========================================================================================
저장한 시간 - 2026-04-20 14:14:50.9973699
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <array>
#include <list>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	save("메인.cpp");
	std::array<int, 5> a{ 1, 2, 3, 4, 5 };

	// at() - 경계를 검사하고 싶다면 이 함수를 사용하면 된다.
	// C++은 속도를 최우선으로 하는 언어이다.
	// 시간걸리는 at()을 다른 함수로 제공하는 이유이다.
	// at()은 예외를 던진다.
	while (true) {
		std::cout << "찾을 원소는?";
		int num;
		std::cin >> num;
		
		try {
			std::cout << a.at(num) << std::endl;
		}
		catch (std::exception& e) {
			std::cout << e.what() << std::endl;	// 표준예외를 출력한다.
		}
	}
}

==========================================================================================
저장한 시간 - 2026-04-20 14:21:57.5226529
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - dynamic (size) array - free-store에 data 관리
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <fstream>
#include <algorithm>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	save("메인.cpp");

	std::vector<ZString> v;

	// [문제] "메인.cpp"에 있는 모든 단어를 v에 저장하라.
	std::ifstream in{ "메인.cpp" };
	if (not in)
		return 0;

	ZString zs;
	while (in >> zs)
		v.push_back(zs);

	std::sort(v.begin(), v.end(), [](const ZString& a, const ZString& b) {
		return a.size() < b.size();
		});

	for (const ZString& zs : v)
		std::cout << zs << std::endl;
}

==========================================================================================
저장한 시간 - 2026-04-20 14:46:33.7443753
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - dynamic (size) array - free-store에 data 관리
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <fstream>
#include <algorithm>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	save("메인.cpp");

	std::vector<int> v{ 1, 2, 3, 4, 5 };

	std::cout << "v의 크기 - " << sizeof v << std::endl;
	std::cout << "v의 주소 - " << addressof(v) << std::endl;
	std::cout << "v의 타입 - " << typeid(v).name() << std::endl;
}

==========================================================================================
저장한 시간 - 2026-04-20 14:47:36.0881152
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - dynamic (size) array - free-store에 data 관리
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <fstream>
#include <algorithm>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	save("메인.cpp");

	std::vector<int> v{ 1, 2, 3, 4, 5 };

	// 이론상 갯수. 실제로는 이만큼 못담음.
	std::cout << "v가 저장할 수 있는 최대 int 개수 - " << v.max_size() << std::endl;
}

==========================================================================================
저장한 시간 - 2026-04-20 14:49:43.0257987
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - dynamic (size) array - free-store에 data 관리
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <fstream>
#include <algorithm>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	save("메인.cpp");

	std::vector<int> v{ 1, 2, 3, 4, 5 };

	std::cout << "v가 차지한 메모리 - " << sizeof(v) << std::endl;
}

==========================================================================================
저장한 시간 - 2026-04-20 15:00:00.0758014
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - dynamic (size) array - free-store에 data 관리
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <fstream>
#include <algorithm>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	save("메인.cpp");

	std::vector<int> v{ 1, 2, 3, 4, 5 };

	// x86 - 12
	// x64 - 24
	// x64 - Debug - 32
	std::cout << "v가 차지한 메모리 - " << sizeof(v) << std::endl;
}

==========================================================================================
저장한 시간 - 2026-04-20 15:06:41.5420989
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - dynamic (size) array - free-store에 data 관리
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include<algorithm>
#include <string>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	save("메인.cpp");

	// [문제] 키보드에서 입력한 모든 단어를 저장하라.
	// 입력의 끝은 OS 마다 지정되어 있다. (ctrl+z)
	// 오름차순으로 정렬하라.
	// 출력하라.

	std::vector<std::string> v;

	std::string s;
	while (std::cin >> s) {
		v.push_back(s);
	}

	for (const std::string& s : v)
		std::cout << s << std::endl;
}

==========================================================================================
저장한 시간 - 2026-04-20 15:07:49.3854933
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - dynamic (size) array - free-store에 data 관리
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include<algorithm>
#include <string>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	save("메인.cpp");

	// [문제] 키보드에서 입력한 모든 단어를 저장하라.
	// 입력의 끝은 OS 마다 지정되어 있다. (ctrl+z)
	// 오름차순으로 정렬하라.
	// 출력하라.

	std::vector<std::string> v;

	std::string s;
	while (std::cin >> s) {
		v.push_back(s);
	}

	// 오름차순(ascending order) 정렬
	std::sort(v.begin(), v.end());

	for (const std::string& s : v)
		std::cout << s << std::endl;
}

==========================================================================================
저장한 시간 - 2026-04-20 15:11:34.7121190
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - dynamic (size) array - free-store에 data 관리
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include<algorithm>
#include <string>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	save("메인.cpp");

	// [문제] 키보드에서 입력한 모든 단어를 저장하라.
	// 입력의 끝은 OS 마다 지정되어 있다. (ctrl+z)
	// 오름차순으로 정렬하라.
	// 출력하라.

	std::vector<std::string> v;

	std::string s;
	while (std::cin >> s) {
		v.push_back(s);
	}

	// 오름차순(ascending order) 정렬
	std::sort(v.begin(), v.end());

	for (auto i = v.rbegin(); i != v.rend(); ++i) {
		std::cout << *i << std::endl;
	}
}

==========================================================================================
저장한 시간 - 2026-04-20 15:14:04.7885899
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - dynamic (size) array - free-store에 data 관리
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include<algorithm>
#include <string>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	save("메인.cpp");

	// [문제] 키보드에서 입력한 모든 단어를 저장하라.
	// 입력의 끝은 OS 마다 지정되어 있다. (ctrl+z)
	// 오름차순으로 정렬하라.
	// 출력하라.
	std::vector<std::string> v{ std::istream_iterator<std::string>{std::cin}, {} };

	// 오름차순(ascending order) 정렬
	std::sort(v.begin(), v.end());

	for (auto i = v.rbegin(); i != v.rend(); ++i) {
		std::cout << *i << std::endl;
	}
}

==========================================================================================
저장한 시간 - 2026-04-20 15:16:50.7158926
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - dynamic (size) array - free-store에 data 관리
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include <string>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	save("메인.cpp");

	// [문제] 키보드에서 입력한 모든 정수의 합계를 출력하라. (중간고사 끝나고?)
	std::vector<int> v{ std::istream_iterator<int>{std::cin}, {} };

	long long sum = std::accumulate(v.begin(), v.end(), 0LL);
	std::cout << sum << std::endl;
}

==========================================================================================
저장한 시간 - 2026-04-20 15:17:49.9607252
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - dynamic (size) array - free-store에 data 관리
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include <string>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	save("메인.cpp");

	// [문제] 키보드에서 입력한 모든 정수의 합계를 출력하라. (중간고사 끝나고?)
	long long sum = std::accumulate(std::istream_iterator<int>{std::cin}, {}, 0LL);
	std::cout << sum << std::endl;
}

==========================================================================================
저장한 시간 - 2026-04-20 15:18:36.4838476
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78		4월 20일													(7주 2일)
// 중간고사 4월 21일 (8주 1일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - dynamic (size) array - free-store에 data 관리
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include <string>
#include "save.h"
#include "ZString.h"

extern bool 관찰;				// 관찰하려면 true

// ----------
int main()
// ----------
{
	save("메인.cpp");

	// [문제] 키보드에서 입력한 모든 정수의 합계를 출력하라. (중간고사 끝나고?)
	std::cout << "합계 - " << std::accumulate(std::istream_iterator<int>{std::cin}, {}, 0LL) << std::endl;
}